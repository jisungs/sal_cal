# 📁 프로젝트 구조 정리 가이드

## ✅ 정리 완료 상태

프로젝트 구조가 `PROJECT_STRUCTURE.md`에 명시된 대로 정리되었습니다.

---

## 📂 최종 프로젝트 구조

```
salary_cal/
├── 📱 데스크톱 앱 파일
│   ├── main.py                 # GUI 메인 진입점 (tkinter)
│   ├── view_dashboard.py       # 대시보드 뷰어
│   ├── demo.py                 # 데모 스크립트
│   ├── build.spec              # PyInstaller 빌드 설정 (공통)
│   ├── build_mac.spec          # macOS 빌드 설정
│   ├── build_win.spec          # Windows 빌드 설정
│   ├── build.sh                # 빌드 스크립트 (Linux/Mac)
│   └── build.bat               # 빌드 스크립트 (Windows)
│
├── 🌐 웹 앱 파일
│   ├── app.py                  # Flask 웹 애플리케이션
│   ├── run_web.sh              # 웹 실행 스크립트 (Linux/Mac)
│   ├── run_web.bat             # 웹 실행 스크립트 (Windows)
│   ├── web/
│   │   ├── templates/          # HTML 템플릿
│   │   │   ├── base.html
│   │   │   ├── index.html
│   │   │   ├── result.html
│   │   │   └── error.html
│   │   ├── static/             # CSS, JS
│   │   │   ├── css/
│   │   │   │   └── style.css
│   │   │   └── js/
│   │   │       └── main.js
│   │   └── uploads/            # 업로드 파일 임시 저장
│   └── outputs/                # 생성된 파일
│       ├── pdf/
│       └── excel/
│
├── 📦 공통 모듈 (두 앱 모두 사용)
│   └── payroll_generator/
│       ├── __init__.py
│       ├── calculator.py       # 급여 계산 로직
│       ├── excel_handler.py    # 엑셀 처리 (템플릿 기반)
│       ├── pdf_generator.py    # PDF 생성 (템플릿 기반)
│       ├── dashboard.py        # 대시보드 분석 (데스크톱 전용)
│       ├── settings.py         # 설정 관리
│       ├── history_manager.py  # 월별 급여 이력 관리
│       ├── config.py           # 설정값 (세율, 상한액 등)
│       ├── utils.py             # 유틸리티 함수
│       ├── logger.py           # 로깅 시스템
│       ├── templates/          # 엑셀 템플릿 파일
│       │   ├── payroll_template.xlsx      # 급여명세서 템플릿
│       │   ├── employee_template.xlsx      # 직원 정보 템플릿
│       │   ├── employee_template_test.xlsx
│       │   └── company_template.xlsx      # 회사 정보 템플릿
│       ├── assets/             # 리소스 파일
│       │   ├── NanumGothic.ttf # 한글 폰트
│       │   └── 나눔 글꼴/      # 추가 폰트 파일들
│       ├── data/               # 데이터 파일
│       │   ├── monthly_payroll_history.xlsx
│       │   └── settings.json
│       ├── docs/               # 문서
│       │   ├── 사용자_매뉴얼.md
│       │   └── 샘플_파일_가이드.md
│       ├── logs/               # 로그 파일
│       │   └── app.log
│       └── output/             # 데스크톱 앱 출력 파일 (기존)
│
└── 📚 문서 및 설정
    ├── README.md               # 프로젝트 메인 문서
    ├── README_WEB.md           # 웹 버전 가이드
    ├── README_STRUCTURE.md     # 구조 정리 가이드 (이 파일)
    ├── PROJECT_STRUCTURE.md    # 프로젝트 구조 문서
    ├── requirements.txt        # 전체 의존성 (데스크톱 + 웹)
    ├── requirements-desktop.txt  # 데스크톱 앱 전용
    ├── requirements-web.txt    # 웹 앱 전용
    ├── .gitignore              # Git 무시 파일
    ├── run_dashboard.sh        # 대시보드 실행 스크립트
    ├── sample/                 # 샘플 파일
    └── plan/                   # 프로젝트 계획 문서
```

---

## 🎯 파일 분류

### 데스크톱 앱 전용
- `main.py` - tkinter GUI 애플리케이션
- `view_dashboard.py` - 대시보드 뷰어
- `demo.py` - 데모 스크립트
- `build.*` - 빌드 관련 파일
- `run_dashboard.sh` - 대시보드 실행 스크립트

### 웹 앱 전용
- `app.py` - Flask 웹 애플리케이션
- `run_web.sh`, `run_web.bat` - 웹 실행 스크립트
- `web/` - 웹 관련 파일 (템플릿, 정적 파일, 업로드)
- `outputs/` - 웹 앱 생성 파일 저장소

### 공통 모듈
- `payroll_generator/` - 두 앱 모두 사용하는 핵심 모듈

---

## 📋 의존성 관리

### 전체 설치 (데스크톱 + 웹)
```bash
pip install -r requirements.txt
```

### 데스크톱 앱만 설치
```bash
pip install -r requirements-desktop.txt
```

### 웹 앱만 설치
```bash
pip install -r requirements-web.txt
```

---

## 🚀 실행 방법

### 데스크톱 앱
```bash
python main.py
```

### 웹 앱
```bash
# Linux/Mac
./run_web.sh

# Windows
run_web.bat

# 또는 직접 실행
python app.py
```

---

## ✅ 정리 완료 체크리스트

- [x] 웹 앱 폴더 구조 생성 (`web/`)
- [x] 출력 폴더 구조 생성 (`outputs/`)
- [x] Flask 애플리케이션 생성 (`app.py`)
- [x] HTML 템플릿 생성 (`web/templates/`)
- [x] 정적 파일 생성 (`web/static/`)
- [x] 실행 스크립트 생성 (`run_web.sh`, `run_web.bat`)
- [x] 의존성 파일 분리 (`requirements-desktop.txt`, `requirements-web.txt`)
- [x] 프로젝트 구조 문서화 (`PROJECT_STRUCTURE.md`)
- [x] Git 무시 파일 설정 (`.gitignore`)

---

## 📝 참고 사항

1. **공통 모듈**: `payroll_generator/` 폴더는 두 앱 모두에서 사용됩니다.
2. **템플릿 파일**: `payroll_generator/templates/`의 템플릿 파일은 두 앱 모두에서 사용됩니다.
3. **출력 파일**: 
   - 데스크톱 앱: `payroll_generator/output/`
   - 웹 앱: `outputs/`
4. **로그 파일**: `payroll_generator/logs/`에 저장됩니다.

---

**작성일**: 2025-01-XX  
**버전**: 1.0

