# GitHub Push 최종 해결 방법

**작성일**: 2025-12-16  
**문제**: Git 히스토리에 큰 파일(125MB)이 남아있어 GitHub push 실패  
**해결**: 얕은 클론으로 새 저장소 생성 (권장)

---

## 🔍 문제 확인

### 확인 결과
- ✅ 현재 추적 중인 파일: 큰 파일 없음
- ❌ Git 히스토리: 큰 파일 포함 (커밋 해시: `99d1108d3b0513f7e921be38fe4483f8fa60dae9`)
- ❌ .git 폴더 크기: **276MB** (큰 파일이 히스토리에 포함됨)

### 문제 원인
- `git rm --cached`로 파일을 제거했지만, **이전 커밋 히스토리에 파일이 남아있음**
- GitHub는 push할 때 **전체 히스토리를 검사**하여 100MB 이상의 파일을 거부
- 히스토리를 재작성하지 않으면 push 불가능

---

## ✅ 해결 방법: 얕은 클론으로 새 저장소 생성

웹 프로젝트 배포에는 이전 히스토리가 필요하지 않으므로, 가장 간단한 방법을 사용합니다.

### 실행 스크립트

다음 명령어를 **순서대로** 실행하세요:

```bash
# ============================================
# 1단계: 백업 (안전을 위해)
# ============================================
cd /Users/jisungs/Documents/dev/sideprojects
cp -r salary_cal salary_cal_backup

# ============================================
# 2단계: 새 Git 저장소 초기화
# ============================================
cd salary_cal

# 기존 Git 히스토리 제거
rm -rf .git

# 새 Git 저장소 초기화
git init

# ============================================
# 3단계: 현재 파일만 추가 (큰 파일 제외 확인)
# ============================================
# 큰 파일이 .gitignore에 포함되어 있는지 확인
cat .gitignore | grep -E "(nanum|zip|\.db)"

# 현재 파일 추가
git add .

# 추가된 파일 확인 (큰 파일이 없는지 확인)
git ls-files | xargs -I {} du -h {} 2>/dev/null | sort -rh | head -10

# ============================================
# 4단계: 초기 커밋 생성
# ============================================
git commit -m "Initial commit: 웹 프로젝트 배포용

- Flask 웹 애플리케이션
- 급여명세서 자동 생성 기능
- 템플릿 디자인 지원 (template_sample1, template_sample2)
- 사용자 인증 시스템 (회원가입, 로그인, 비밀번호 재설정)
- 웹 직접 입력 기능 (단일/다중 직원)
- 데이터 수집 및 분석 기능"

# ============================================
# 5단계: 원격 저장소 연결
# ============================================
# 기존 원격 저장소 제거 (있다면)
git remote remove origin 2>/dev/null || true

# 새 원격 저장소 추가
git remote add origin https://github.com/jisungs/sal_cal.git

# 원격 저장소 확인
git remote -v

# ============================================
# 6단계: 브랜치 이름 설정
# ============================================
git branch -M main

# ============================================
# 7단계: Force Push (기존 내용 덮어쓰기)
# ============================================
git push -u origin main --force
```

---

## 📋 단계별 설명

### 1단계: 백업
- 현재 프로젝트를 `salary_cal_backup`으로 백업
- 문제 발생 시 복구 가능

### 2단계: 새 저장소 초기화
- `.git` 폴더 삭제로 모든 히스토리 제거
- 새 Git 저장소 초기화

### 3단계: 파일 추가
- 현재 작업 디렉토리의 파일만 추가
- `.gitignore`에 의해 큰 파일은 자동 제외
- 추가된 파일 확인으로 큰 파일이 없는지 검증

### 4단계: 초기 커밋
- 현재 상태를 초기 커밋으로 생성
- 히스토리 없이 깨끗한 시작

### 5-7단계: 원격 저장소 연결 및 Push
- GitHub 원격 저장소 연결
- Force push로 기존 내용 덮어쓰기

---

## 🎯 예상 결과

### 성공 시 출력
```
Enumerating objects: XXXX, done.
Counting objects: 100% (XXXX/XXXX), done.
Delta compression using up to 8 threads
Compressing objects: 100% (XXXX/XXXX), done.
Writing objects: 100% (XXXX/XXXX), XX.XX MiB | XX.XX MiB/s, done.
Total XXXX (delta XXXX), reused 0 (delta 0)
To https://github.com/jisungs/sal_cal.git
 + [remote rejected] main -> main (pre-receive hook declined)
 * [new branch]      main -> main
Branch 'main' set up to track 'remote branch 'main' from 'origin'.
```

**참고**: `[remote rejected]` 메시지는 기존 브랜치를 덮어쓰는 과정에서 나타날 수 있지만, `[new branch]` 메시지가 나오면 성공입니다.

### 저장소 크기
- **이전**: 274.35 MiB (큰 파일 포함)
- **이후**: 약 50-100 MB (큰 파일 제외)

---

## ⚠️ 주의사항

1. **백업 필수**
   - 히스토리를 잃을 수 있으므로 반드시 백업하세요
   - `salary_cal_backup` 폴더에 백업 권장

2. **Force Push**
   - `--force` 옵션은 원격 저장소의 기존 내용을 완전히 덮어씁니다
   - 다른 사람과 협업 중이라면 주의하세요
   - 웹 프로젝트만 배포하는 경우 문제없음

3. **파일 확인**
   - 큰 파일이 제대로 제외되었는지 확인하세요
   - `git ls-files | xargs du -h | sort -rh | head -10` 명령어로 확인

4. **원격 저장소**
   - GitHub 저장소가 비어있거나 덮어써도 되는지 확인하세요
   - 중요한 내용이 있다면 먼저 백업하세요

---

## 🔧 문제 해결 체크리스트

실행 전 확인:
- [ ] 백업 완료 (`salary_cal_backup` 폴더 생성)
- [ ] `.gitignore`에 큰 파일 제외 확인
- [ ] 현재 작업 디렉토리 확인 (`/Users/jisungs/Documents/dev/sideprojects/salary_cal`)

실행 중 확인:
- [ ] `git add .` 후 큰 파일이 추가되지 않았는지 확인
- [ ] `git ls-files`로 큰 파일이 추적되지 않는지 확인
- [ ] 초기 커밋 메시지 확인

실행 후 확인:
- [ ] GitHub에서 파일 확인
- [ ] 큰 파일이 없는지 확인
- [ ] README.md가 올바르게 표시되는지 확인

---

## 🚀 빠른 실행 (한 번에)

다음 명령어를 복사하여 한 번에 실행할 수 있습니다:

```bash
cd /Users/jisungs/Documents/dev/sideprojects/salary_cal && \
cp -r ../salary_cal ../salary_cal_backup && \
rm -rf .git && \
git init && \
git add . && \
git commit -m "Initial commit: 웹 프로젝트 배포용" && \
git remote remove origin 2>/dev/null || true && \
git remote add origin https://github.com/jisungs/sal_cal.git && \
git branch -M main && \
git push -u origin main --force
```

---

## 📊 대안 방법

### 방법 2: Git LFS 사용 (큰 파일 유지 필요 시)

큰 파일을 유지해야 하는 경우:

```bash
# Git LFS 설치
brew install git-lfs

# Git LFS 초기화
git lfs install

# 큰 파일을 LFS로 추적
git lfs track "*.zip"
git lfs track "payroll_generator/assets/nanum-all_new.zip"

# .gitattributes 파일 커밋
git add .gitattributes
git commit -m "chore: Git LFS 설정"

# 히스토리에서 큰 파일을 LFS로 마이그레이션
git lfs migrate import --include="payroll_generator/assets/nanum-all_new.zip" --everything

# Push
git push -u origin main --force
```

**주의**: Git LFS는 GitHub에서 무료로 제공하지만 용량 제한이 있습니다.

---

**다음 단계**: 위의 실행 스크립트를 순서대로 실행하여 새 저장소로 push하세요!

