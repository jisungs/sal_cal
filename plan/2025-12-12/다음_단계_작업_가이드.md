# 🚀 디자인 선택 기능 추가 - 다음 단계 작업 가이드

**작성일**: 2025-12-12  
**현재 상태**: master 브랜치, 초기 설정 필요  
**다음 단계**: Git 브랜치 설정 및 Phase 0 시작

---

## 📊 현재 상태 확인

### Git 상태
- ✅ 현재 브랜치: `master`
- ⚠️ 변경사항: `app.db` 수정됨
- ⚠️ 추적되지 않는 파일: `plan/2025-12-12/`, `sample/`

### 프로젝트 구조
- ✅ `payroll_generator/templates/` 폴더 존재
- ❌ `payroll_generator/templates/designs/` 폴더 없음 (생성 필요)

---

## 🎯 다음 단계 작업

### Step 1: 현재 변경사항 커밋 (필수)

현재 작업한 실행계획 문서와 샘플 이미지를 커밋합니다.

```bash
# 현재 위치 확인
cd /Users/jisungs/Documents/dev/sideprojects/salary_cal

# 실행계획 문서 및 샘플 이미지 추가
git add plan/2025-12-12/
git add sample/

# 커밋
git commit -m "docs: 디자인 선택 기능 실행계획 및 Git 브랜치 전략 추가

- 디자인 선택 기능 추가 실행계획 작성 (v2.1)
- Git 브랜치 전략 포함 실행계획 작성
- 샘플 이미지 추가 (급여명세서_sample.png, 급여명세서_sample2.png)
- 실행계획 개선 요약 문서 작성"

# 커밋 확인
git log --oneline -1
```

**참고**: `app.db`는 데이터베이스 파일이므로 `.gitignore`에 추가하는 것을 권장합니다.

---

### Step 2: Git 브랜치 설정 (필수)

#### 2.1 develop 브랜치 생성

```bash
# develop 브랜치 생성 및 전환
git checkout -b develop

# 원격 저장소에 푸시 (원격 저장소가 있다면)
git push -u origin develop
```

#### 2.2 feature/design-selection 브랜치 생성

```bash
# 기능 브랜치 생성 및 전환
git checkout -b feature/design-selection

# 원격 저장소에 푸시 (원격 저장소가 있다면)
git push -u origin feature/design-selection
```

#### 2.3 브랜치 확인

```bash
# 현재 브랜치 확인
git branch

# 모든 브랜치 확인
git branch -a
```

**예상 출력**:
```
* feature/design-selection
  develop
  master
```

---

### Step 3: Phase 0 브랜치 생성 및 작업 시작

#### 3.1 Phase 0 브랜치 생성

```bash
# feature/design-selection 브랜치에서 Phase 0 브랜치 생성
git checkout feature/design-selection
git checkout -b feature/design-selection-phase-0
```

#### 3.2 디렉토리 구조 생성

```bash
# 디자인 시스템 디렉토리 생성
mkdir -p payroll_generator/templates/designs/configs

# __init__.py 파일 생성
touch payroll_generator/templates/designs/__init__.py
touch payroll_generator/templates/designs/configs/.gitkeep
```

#### 3.3 샘플 이미지 분석 및 설정 파일 작성

**작업 내용**:
1. `sample/급여명세서_sample.png` 분석
   - 레이아웃 구조 파악
   - 색상 코드 추출 (RGB 값)
   - 폰트 크기 측정
   - 간격 및 여백 측정

2. `sample/급여명세서_sample2.png` 분석
   - 동일한 방식으로 분석

3. `payroll_generator/templates/designs/configs/design_1.yaml` 작성
   - 샘플 이미지 분석 결과를 YAML 형식으로 작성
   - 레이아웃 위치, 색상, 폰트 등 모든 스펙 정의

4. `payroll_generator/templates/designs/configs/design_2.yaml` 작성
   - 동일한 방식으로 작성

**설정 파일 구조 예시** (`design_1.yaml`):
```yaml
design_name: "디자인 1"
description: "급여명세서_sample.png 기반"
version: "1.0"

# 페이지 설정
page:
  size: "A4"
  orientation: "portrait"
  margins:
    top: 30
    bottom: 20
    left: 30
    right: 30
    unit: "mm"

# 폰트 설정
fonts:
  title:
    name: "NanumGothic"
    size: 22
    bold: true
    color: [0, 0, 0]
  header:
    name: "NanumGothic"
    size: 12
    bold: true
    color: [1, 1, 1]
  body:
    name: "NanumGothic"
    size: 10
    bold: false
    color: [0, 0, 0]

# 색상 설정 (RGB 0-1 범위)
colors:
  title_bg: [0.95, 0.95, 0.95]
  header_bg: [0.2, 0.4, 0.8]
  total_bg: [0.9, 0.95, 1.0]
  border: [0.7, 0.7, 0.7]

# 레이아웃 설정 (mm 단위, 상단에서)
layout:
  title:
    y: 30
    height: 20
    left: 30
    right: 30
  employee_info:
    y: 70
    height: 50
    fields:
      - name: "이름"
        y_offset: 0
      - name: "주민번호"
        y_offset: -12
      - name: "입사일"
        y_offset: -24
  # ... 나머지 섹션
```

**참고**: 상세한 설정 파일 구조는 `디자인_선택_기능_추가_실행계획_v2.md` 파일의 "설정 파일 구조 상세" 섹션을 참조하세요.

#### 3.4 Phase 0 커밋 및 병합

```bash
# 변경사항 확인
git status

# 설정 파일 추가
git add payroll_generator/templates/designs/

# 커밋
git commit -m "feat(design): Phase 0 - 디자인 설정 파일 작성

- 급여명세서_sample.png 분석 결과를 design_1.yaml로 작성
- 급여명세서_sample2.png 분석 결과를 design_2.yaml로 작성
- 레이아웃, 색상, 폰트 스펙 정의
- 디자인 시스템 디렉토리 구조 생성"

# 원격 저장소에 푸시 (원격 저장소가 있다면)
git push -u origin feature/design-selection-phase-0

# feature/design-selection에 병합
git checkout feature/design-selection
git merge feature/design-selection-phase-0 --no-ff -m "merge: Phase 0 완료 - 설정 파일 작성"
git push origin feature/design-selection

# Phase 0 브랜치 삭제 (선택사항)
git branch -d feature/design-selection-phase-0
git push origin --delete feature/design-selection-phase-0  # 원격 저장소가 있다면
```

---

## 📋 작업 체크리스트

### Step 1: 현재 변경사항 커밋
- [ ] `plan/2025-12-12/` 폴더 추가
- [ ] `sample/` 폴더 추가
- [ ] 커밋 메시지 작성
- [ ] 커밋 완료

### Step 2: Git 브랜치 설정
- [ ] develop 브랜치 생성
- [ ] feature/design-selection 브랜치 생성
- [ ] 브랜치 확인

### Step 3: Phase 0 작업
- [ ] Phase 0 브랜치 생성
- [ ] 디렉토리 구조 생성
- [ ] 샘플 이미지 분석
- [ ] `design_1.yaml` 작성
- [ ] `design_2.yaml` 작성
- [ ] Phase 0 커밋
- [ ] feature/design-selection에 병합

---

## 🔍 다음 단계 (Phase 0 완료 후)

Phase 0가 완료되면 다음 단계로 진행합니다:

### Phase 1: 기본 구조 구축
- BaseDesign 클래스 구현
- DesignFactory 클래스 구현
- PyYAML 의존성 추가

**상세 내용**: `디자인_선택_기능_추가_실행계획.md` 파일의 "Phase 1" 섹션 참조

---

## ⚠️ 주의사항

### 1. app.db 파일 처리
`app.db`는 데이터베이스 파일이므로 커밋하지 않는 것을 권장합니다.

```bash
# .gitignore에 추가 (이미 있다면 확인)
echo "app.db" >> .gitignore

# 또는 기존 .gitignore 확인
cat .gitignore | grep app.db
```

### 2. 원격 저장소가 없는 경우
원격 저장소가 없다면 `git push` 명령어는 생략하고 로컬에서만 작업하세요.

### 3. 충돌 발생 시
병합 시 충돌이 발생하면:
1. 충돌 파일 확인: `git status`
2. 충돌 해결: 파일 편집
3. 해결 확인: `git add <file>`
4. 병합 완료: `git commit`

---

## 📚 참고 문서

- **실행계획**: `디자인_선택_기능_추가_실행계획.md`
- **Git 브랜치 전략**: `Git_브랜치_전략_포함_실행계획.md`
- **상세 구현 코드**: `디자인_선택_기능_추가_실행계획_v2.md`

---

## 🚀 빠른 시작 명령어

전체 과정을 한 번에 실행하려면:

```bash
# Step 1: 현재 변경사항 커밋
cd /Users/jisungs/Documents/dev/sideprojects/salary_cal
git add plan/2025-12-12/ sample/
git commit -m "docs: 디자인 선택 기능 실행계획 및 Git 브랜치 전략 추가"

# Step 2: 브랜치 설정
git checkout -b develop
git checkout -b feature/design-selection

# Step 3: Phase 0 시작
git checkout -b feature/design-selection-phase-0
mkdir -p payroll_generator/templates/designs/configs
touch payroll_generator/templates/designs/__init__.py

# 이제 설정 파일 작성 작업 시작!
```

---

**작성자**: AI Assistant  
**작성일**: 2025-12-12  
**상태**: ✅ 다음 단계 작업 가이드 작성 완료
