# 🔍 권한 오류 분석 보고서

**작성일**: 2025-12-11  
**에러**: `zsh: permission denied: tests/test_app_init.py`  
**상태**: ✅ 원인 분석 완료, 해결 방법 제시

---

## 🔍 문제 상황

테스트 파일을 직접 실행하려고 할 때 권한 오류가 발생했습니다.

```
tests/test_app_init.py
zsh: permission denied: tests/test_app_init.py
```

---

## 📊 원인 분석

### 파일 권한 확인

```bash
$ ls -la tests/test_app_init.py
-rw-r--r--@ 1 jisungs  staff  3862 Dec 11 10:58 tests/test_app_init.py
```

### 권한 해석

- **`-rw-r--r--`**: 파일 권한
  - 소유자(jisungs): `rw-` (읽기/쓰기만 가능, 실행 권한 없음)
  - 그룹(staff): `r--` (읽기만 가능)
  - 기타: `r--` (읽기만 가능)

### 문제 원인

1. **파일에 실행 권한이 없음**: `-rw-r--r--`에서 실행 권한(`x`)이 없습니다.
2. **직접 실행 시도**: `tests/test_app_init.py`를 직접 실행하려고 했습니다.
3. **Shebang 존재**: 파일 첫 줄에 `#!/usr/bin/env python3`가 있어서 실행 가능한 스크립트처럼 보이지만, 실행 권한이 없어서 실행할 수 없습니다.

---

## 💡 해결 방법

### 방법 1: Python 인터프리터로 실행 (권장) ⭐

**가장 일반적이고 권장되는 방법**:

```bash
python tests/test_app_init.py
```

또는:

```bash
python3 tests/test_app_init.py
```

**장점**:
- 실행 권한이 필요 없음
- 모든 Python 파일에 적용 가능
- 표준적인 실행 방법

### 방법 2: 실행 권한 부여

파일에 실행 권한을 부여한 후 직접 실행:

```bash
chmod +x tests/test_app_init.py
./tests/test_app_init.py
```

**장점**:
- 직접 실행 가능
- shebang이 있으면 자동으로 Python 인터프리터 사용

**단점**:
- 파일마다 권한 부여 필요
- Git에서 권한 변경 추적 필요

### 방법 3: 테스트 러너 사용

프로젝트에 있는 테스트 러너 사용:

```bash
python tests/run_all_tests.py
```

**장점**:
- 모든 테스트를 한 번에 실행
- 일관된 실행 환경

---

## 🎯 권장 해결 방법

**방법 1 (Python 인터프리터로 실행)**을 권장합니다.

이유:
1. 실행 권한이 필요 없음
2. 표준적인 Python 파일 실행 방법
3. 모든 환경에서 동일하게 작동
4. Git에서 권한 변경 추적 불필요

---

## 📝 실행 예시

### 올바른 실행 방법

```bash
# 가상환경 활성화 (필요한 경우)
source venv/bin/activate

# Python 인터프리터로 실행
python tests/test_app_init.py
```

### 다른 테스트 파일들도 동일하게

```bash
python tests/test_config.py
python tests/test_cleanup.py
python tests/test_error_handlers.py

# 또는 모든 테스트 실행
python tests/run_all_tests.py
```

---

## 🔧 추가 권장 사항

### 테스트 실행 스크립트 활용

프로젝트에 `tests/run_all_tests.py`가 있으므로 이를 활용하는 것이 좋습니다:

```bash
python tests/run_all_tests.py
```

이 스크립트는 모든 테스트를 순차적으로 실행하고 결과를 요약해줍니다.

---

## ✅ 결론

**원인**: 파일에 실행 권한이 없어서 직접 실행할 수 없습니다.

**해결**: Python 인터프리터를 사용하여 실행하세요:

```bash
python tests/test_app_init.py
```

**참고**: 모든 Python 테스트 파일은 동일한 방식으로 실행할 수 있습니다.

---

**작성자**: AI Assistant  
**작성일**: 2025-12-11  
**상태**: ✅ 원인 분석 완료, 해결 방법 제시
