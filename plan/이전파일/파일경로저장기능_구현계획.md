# 📋 파일 경로 저장 기능 구현 계획

**작성일**: 2025-11-11  
**요청 사항**: 프로그램 첫 실행 시 직원 정보 엑셀 파일 입력 및 경로 저장 기능

---

## 📊 현재 상태 분석

### ✅ 현재 구현된 기능

1. **기본 파일 자동 로드**
   - `load_default_dashboard_file()` 메서드 존재
   - 템플릿 파일(`payroll_generator/templates/employee_template.xlsx`) 자동 로드
   - 프로그램 시작 시 자동 실행

2. **파일 선택 기능**
   - `load_file_for_dashboard()` 메서드로 파일 선택 다이얼로그 제공
   - 사용자가 직접 파일 선택 가능

3. **파일 경로 변수**
   - `self.employee_file_path = tk.StringVar()` - 급여명세서 생성 탭용
   - 대시보드용 파일 경로는 메서드 내부에서만 사용

### ❌ 현재 미구현 기능

1. **파일 경로 저장 기능**
   - 선택한 파일 경로를 저장하는 기능 없음
   - 설정 파일이나 JSON 파일로 저장하는 기능 없음

2. **저장된 경로 자동 로드**
   - 다음 실행 시 저장된 경로를 자동으로 로드하는 기능 없음
   - 현재는 항상 템플릿 파일을 자동 로드

3. **첫 실행 감지**
   - 프로그램이 처음 실행되었는지 확인하는 기능 없음
   - 첫 실행 시 파일 선택 다이얼로그를 표시하는 기능 없음

---

## 🎯 구현 목표

### 주요 목표

1. **첫 실행 시 파일 입력**
   - 프로그램이 처음 실행되면 파일 선택 다이얼로그 표시
   - 사용자가 직원 정보 엑셀 파일 선택

2. **파일 경로 저장**
   - 선택한 파일 경로를 설정 파일에 저장
   - JSON 형식으로 저장 (읽기/쓰기 용이)

3. **저장된 경로 자동 로드**
   - 다음 실행 시 저장된 파일 경로 자동 로드
   - 파일이 존재하면 자동으로 대시보드 업데이트
   - 파일이 없으면 파일 선택 다이얼로그 표시

---

## 📋 구현 계획

### 1. 설정 파일 관리 모듈 생성

#### 1.1 파일 구조
- **파일명**: `payroll_generator/settings.py`
- **설정 파일 위치**: `payroll_generator/data/settings.json`
- **설정 파일 형식**: JSON

#### 1.2 설정 파일 구조
```json
{
    "last_employee_file": "/path/to/employee_file.xlsx",
    "last_output_folder": "./payroll_generator/output",
    "last_period": "2025-01",
    "last_output_format": "both",
    "is_first_run": false
}
```

#### 1.3 SettingsManager 클래스
```python
class SettingsManager:
    def __init__(self):
        self.settings_file = 'payroll_generator/data/settings.json'
        self.settings = self.load_settings()
    
    def load_settings(self):
        """설정 파일 로드"""
        # 파일이 없으면 기본값 반환
        # 파일이 있으면 JSON 로드
    
    def save_settings(self):
        """설정 파일 저장"""
        # JSON 형식으로 저장
    
    def get_last_employee_file(self):
        """마지막으로 사용한 직원 파일 경로 반환"""
    
    def set_last_employee_file(self, file_path):
        """마지막으로 사용한 직원 파일 경로 저장"""
    
    def is_first_run(self):
        """첫 실행 여부 확인"""
    
    def set_first_run_complete(self):
        """첫 실행 완료 표시"""
```

### 2. main.py 수정

#### 2.1 SettingsManager 통합
- `SettingsManager` 인스턴스 생성
- 설정 파일에서 마지막 파일 경로 로드

#### 2.2 첫 실행 처리
- `is_first_run()` 확인
- 첫 실행이면 파일 선택 다이얼로그 표시
- 파일 선택 후 경로 저장

#### 2.3 자동 로드 로직
- 저장된 파일 경로 확인
- 파일이 존재하면 자동 로드
- 파일이 없으면 파일 선택 다이얼로그 표시

#### 2.4 파일 선택 시 저장
- 사용자가 파일을 선택하면 즉시 설정 파일에 저장
- 다음 실행 시 자동으로 로드되도록 함

---

## 🔄 구현 단계

### 단계 1: SettingsManager 클래스 생성
**예상 시간**: 30분

**작업 내용**:
- [ ] `payroll_generator/settings.py` 파일 생성
- [ ] `SettingsManager` 클래스 구현
  - [ ] `load_settings()` 메서드
  - [ ] `save_settings()` 메서드
  - [ ] `get_last_employee_file()` 메서드
  - [ ] `set_last_employee_file()` 메서드
  - [ ] `is_first_run()` 메서드
  - [ ] `set_first_run_complete()` 메서드
- [ ] 에러 처리 (파일 없음, JSON 파싱 오류 등)

### 단계 2: main.py 통합
**예상 시간**: 1시간

**작업 내용**:
- [ ] `SettingsManager` import 및 인스턴스 생성
- [ ] `__init__` 메서드에서 설정 로드
- [ ] `load_default_dashboard_file()` 메서드 수정
  - [ ] 첫 실행 확인
  - [ ] 첫 실행이면 파일 선택 다이얼로그 표시
  - [ ] 저장된 경로가 있으면 자동 로드
  - [ ] 저장된 경로가 없거나 파일이 없으면 파일 선택 다이얼로그 표시
- [ ] `load_file_for_dashboard()` 메서드 수정
  - [ ] 파일 선택 후 설정 파일에 저장
  - [ ] `set_first_run_complete()` 호출

### 단계 3: 급여명세서 생성 탭 연동
**예상 시간**: 30분

**작업 내용**:
- [ ] 급여명세서 생성 탭에서도 저장된 경로 사용
- [ ] 파일 선택 시 설정 파일에 저장
- [ ] 출력 폴더 경로도 저장 (선택사항)

### 단계 4: 테스트
**예상 시간**: 30분

**테스트 항목**:
- [ ] 첫 실행 시 파일 선택 다이얼로그 표시 확인
- [ ] 파일 선택 후 설정 파일 저장 확인
- [ ] 다음 실행 시 저장된 파일 자동 로드 확인
- [ ] 저장된 파일이 없을 때 처리 확인
- [ ] 저장된 파일 경로가 유효하지 않을 때 처리 확인

### 단계 5: 에러 처리 및 예외 상황
**예상 시간**: 30분

**작업 내용**:
- [ ] 설정 파일 읽기/쓰기 권한 오류 처리
- [ ] JSON 파싱 오류 처리
- [ ] 저장된 파일이 삭제된 경우 처리
- [ ] 저장된 파일 경로가 유효하지 않은 경우 처리

---

## 📝 구현 상세

### SettingsManager 클래스 상세

```python
# payroll_generator/settings.py
import json
import os
from pathlib import Path

class SettingsManager:
    def __init__(self):
        """설정 관리자 초기화"""
        # 설정 파일 경로
        settings_dir = Path(__file__).parent / 'data'
        settings_dir.mkdir(exist_ok=True)
        self.settings_file = settings_dir / 'settings.json'
        
        # 기본 설정
        self.default_settings = {
            'last_employee_file': '',
            'last_output_folder': './payroll_generator/output',
            'last_period': '2025-01',
            'last_output_format': 'both',
            'is_first_run': True
        }
        
        # 설정 로드
        self.settings = self.load_settings()
    
    def load_settings(self):
        """설정 파일 로드"""
        try:
            if self.settings_file.exists():
                with open(self.settings_file, 'r', encoding='utf-8') as f:
                    settings = json.load(f)
                    # 기본값과 병합 (새로운 키 추가 대응)
                    merged = self.default_settings.copy()
                    merged.update(settings)
                    return merged
            else:
                return self.default_settings.copy()
        except Exception as e:
            logger.warning(f"설정 파일 로드 오류: {e}. 기본값 사용")
            return self.default_settings.copy()
    
    def save_settings(self):
        """설정 파일 저장"""
        try:
            with open(self.settings_file, 'w', encoding='utf-8') as f:
                json.dump(self.settings, f, ensure_ascii=False, indent=2)
            logger.info(f"설정 파일 저장 완료: {self.settings_file}")
        except Exception as e:
            logger.error(f"설정 파일 저장 오류: {e}")
            raise
    
    def get_last_employee_file(self):
        """마지막으로 사용한 직원 파일 경로 반환"""
        return self.settings.get('last_employee_file', '')
    
    def set_last_employee_file(self, file_path):
        """마지막으로 사용한 직원 파일 경로 저장"""
        self.settings['last_employee_file'] = file_path
        self.save_settings()
    
    def is_first_run(self):
        """첫 실행 여부 확인"""
        return self.settings.get('is_first_run', True)
    
    def set_first_run_complete(self):
        """첫 실행 완료 표시"""
        self.settings['is_first_run'] = False
        self.save_settings()
    
    def get_last_output_folder(self):
        """마지막으로 사용한 출력 폴더 경로 반환"""
        return self.settings.get('last_output_folder', './payroll_generator/output')
    
    def set_last_output_folder(self, folder_path):
        """마지막으로 사용한 출력 폴더 경로 저장"""
        self.settings['last_output_folder'] = folder_path
        self.save_settings()
```

### main.py 수정 상세

#### 1. SettingsManager 통합
```python
from payroll_generator.settings import SettingsManager

class PayrollApp:
    def __init__(self, root):
        # ...
        self.settings_manager = SettingsManager()
        # ...
```

#### 2. load_default_dashboard_file() 수정
```python
def load_default_dashboard_file(self):
    """기본 파일 로드 (저장된 경로 또는 파일 선택)"""
    # 첫 실행 확인
    if self.settings_manager.is_first_run():
        # 첫 실행이면 파일 선택 다이얼로그 표시
        filename = filedialog.askopenfilename(
            title="직원 정보 엑셀 파일 선택",
            filetypes=[("Excel files", "*.xlsx *.xls"), ("All files", "*.*")]
        )
        if filename:
            # 파일 경로 저장
            self.settings_manager.set_last_employee_file(filename)
            self.settings_manager.set_first_run_complete()
            # 파일 로드
            self.load_dashboard_file(filename, show_message=False)
        else:
            # 파일을 선택하지 않으면 기본 템플릿 로드
            default_path = 'payroll_generator/templates/employee_template.xlsx'
            if os.path.exists(default_path):
                self.load_dashboard_file(default_path, show_message=False)
    else:
        # 저장된 파일 경로 확인
        last_file = self.settings_manager.get_last_employee_file()
        if last_file and os.path.exists(last_file):
            # 저장된 파일이 있으면 자동 로드
            self.load_dashboard_file(last_file, show_message=False)
        else:
            # 저장된 파일이 없거나 삭제된 경우 파일 선택 다이얼로그 표시
            filename = filedialog.askopenfilename(
                title="직원 정보 엑셀 파일 선택",
                filetypes=[("Excel files", "*.xlsx *.xls"), ("All files", "*.*")]
            )
            if filename:
                self.settings_manager.set_last_employee_file(filename)
                self.load_dashboard_file(filename, show_message=False)
            else:
                # 파일을 선택하지 않으면 기본 템플릿 로드
                default_path = 'payroll_generator/templates/employee_template.xlsx'
                if os.path.exists(default_path):
                    self.load_dashboard_file(default_path, show_message=False)
```

#### 3. load_file_for_dashboard() 수정
```python
def load_file_for_dashboard(self):
    """대시보드용 파일 선택"""
    filename = filedialog.askopenfilename(
        title="직원 정보 엑셀 파일 선택",
        filetypes=[("Excel files", "*.xlsx *.xls"), ("All files", "*.*")]
    )
    if filename:
        # 파일 경로 저장
        self.settings_manager.set_last_employee_file(filename)
        self.settings_manager.set_first_run_complete()
        # 파일 로드
        self.load_dashboard_file(filename, show_message=True)
```

---

## ⚠️ 주의사항

### 1. 파일 경로 저장
- 절대 경로로 저장 (상대 경로는 프로그램 위치에 따라 달라질 수 있음)
- 파일 경로 정규화 (`os.path.normpath`, `os.path.abspath`)

### 2. 파일 존재 확인
- 저장된 파일 경로를 로드하기 전에 파일 존재 여부 확인
- 파일이 없으면 파일 선택 다이얼로그 표시

### 3. 에러 처리
- 설정 파일 읽기/쓰기 권한 오류 처리
- JSON 파싱 오류 처리
- 파일 경로가 유효하지 않은 경우 처리

### 4. .gitignore 설정
- `payroll_generator/data/settings.json` 파일은 .gitignore에 추가
- 사용자별 설정이므로 버전 관리에서 제외

---

## 📊 예상 작업 시간

| 단계 | 작업 내용 | 예상 시간 |
|------|----------|----------|
| 1 | SettingsManager 클래스 생성 | 30분 |
| 2 | main.py 통합 | 1시간 |
| 3 | 급여명세서 생성 탭 연동 | 30분 |
| 4 | 테스트 | 30분 |
| 5 | 에러 처리 및 예외 상황 | 30분 |
| **총계** | | **3시간** |

---

## ✅ 체크리스트

### 구현 전
- [ ] 현재 상태 분석 완료
- [ ] 구현 계획 작성 완료

### 구현 중
- [ ] SettingsManager 클래스 생성
- [ ] main.py 통합
- [ ] 급여명세서 생성 탭 연동
- [ ] 테스트
- [ ] 에러 처리

### 구현 후
- [ ] .gitignore 업데이트
- [ ] 문서 업데이트
- [ ] 사용자 매뉴얼 업데이트

---

## 🔗 관련 파일

- `payroll_generator/settings.py` (신규 생성 예정)
- `payroll_generator/data/settings.json` (신규 생성 예정)
- `main.py` (수정 예정)
- `.gitignore` (수정 예정)

---

**마지막 업데이트**: 2025-11-11

