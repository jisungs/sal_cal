# 📋 메뉴바 추가 구현 계획

**작성일**: 2025-11-11  
**요청 사항**: GUI 창에 메뉴바 추가

---

## 📊 현재 상태 분석

### ✅ 현재 구현된 기능

1. **GUI 구조**
   - Tkinter 기반 GUI
   - 탭 구조 (대시보드, 급여명세서 생성)
   - 버튼 기반 파일 선택 기능
   - 모던 디자인 시스템 적용

2. **파일 관련 기능**
   - `load_file_for_dashboard()`: 대시보드 파일 선택
   - `select_employee_file()`: 급여명세서 생성 탭 파일 선택
   - `select_output_folder()`: 출력 폴더 선택
   - `open_output_folder()`: 출력 폴더 열기

3. **설정 관리**
   - `SettingsManager`: 설정 파일 관리
   - 파일 경로 저장/로드
   - 첫 실행 감지

### ❌ 현재 미구현 기능

1. **메뉴바**
   - 메뉴바가 전혀 없음
   - 모든 기능이 버튼으로만 제공됨

2. **키보드 단축키**
   - 단축키 기능 없음

3. **시스템 메뉴 통합**
   - macOS/Windows 시스템 메뉴 통합 없음

---

## 🎯 구현 목표

### 주요 목표

1. **메뉴바 추가**
   - 파일 메뉴
   - 설정 메뉴
   - 도움말 메뉴

2. **키보드 단축키 지원**
   - 일반적인 단축키 제공 (Ctrl+O, Ctrl+Q 등)

3. **기존 기능과의 통합**
   - 기존 버튼 기능을 메뉴에서도 접근 가능하도록
   - 일관된 사용자 경험 제공

---

## 📋 구현 계획

### 1. 메뉴바 구조 설계

#### 1.1 메뉴 구성

```
메뉴바
├── 파일 (File)
│   ├── 직원 정보 파일 열기... (Ctrl+O)
│   ├── 출력 폴더 열기... (Ctrl+Shift+O)
│   ├── ───────────── (구분선)
│   └── 종료 (Ctrl+Q)
│
├── 설정 (Settings)
│   ├── 설정 초기화...
│   ├── 첫 실행 상태로 되돌리기...
│   └── ───────────── (구분선)
│   └── 출력 폴더 설정...
│
└── 도움말 (Help)
    ├── 사용자 매뉴얼 (F1)
    ├── 샘플 파일 가이드
    ├── ───────────── (구분선)
    └── 정보... (Ctrl+I)
```

#### 1.2 메뉴 항목 상세

**파일 메뉴**
- **직원 정보 파일 열기...** (`Ctrl+O`)
  - 기능: 대시보드 탭으로 전환하고 파일 선택 다이얼로그 표시
  - 메서드: `load_file_for_dashboard()` 호출
  
- **출력 폴더 열기...** (`Ctrl+Shift+O`)
  - 기능: 출력 폴더 선택 다이얼로그 표시
  - 메서드: `select_output_folder()` 호출
  
- **종료** (`Ctrl+Q` / `Cmd+Q`)
  - 기능: 프로그램 종료
  - 메서드: `self.root.quit()` 또는 `self.root.destroy()`

**설정 메뉴**
- **설정 초기화...**
  - 기능: 설정 파일 삭제 확인 후 초기화
  - 메서드: `reset_settings()` (신규 구현)
  
- **첫 실행 상태로 되돌리기...**
  - 기능: 첫 실행 플래그를 True로 설정
  - 메서드: `reset_first_run()` (신규 구현)
  
- **출력 폴더 설정...**
  - 기능: 출력 폴더 선택 다이얼로그 표시
  - 메서드: `select_output_folder()` 호출

**도움말 메뉴**
- **사용자 매뉴얼** (`F1`)
  - 기능: 사용자 매뉴얼 파일 열기
  - 파일: `payroll_generator/docs/사용자_매뉴얼.md`
  - 메서드: `open_user_manual()` (신규 구현)
  
- **샘플 파일 가이드**
  - 기능: 샘플 파일 가이드 파일 열기
  - 파일: `payroll_generator/docs/샘플_파일_가이드.md`
  - 메서드: `open_sample_guide()` (신규 구현)
  
- **정보...** (`Ctrl+I` / `Cmd+I`)
  - 기능: 프로그램 정보 다이얼로그 표시
  - 메서드: `show_about()` (신규 구현)

### 2. 구현 상세

#### 2.1 메뉴바 생성

**위치**: `main.py`의 `create_widgets()` 메서드 내부, 탭 노트북 생성 전

```python
def create_widgets(self):
    """위젯 생성"""
    # 메뉴바 생성
    self.create_menu_bar()
    
    # 탭 노트북 생성
    self.notebook = ttk.Notebook(self.root)
    # ...
```

#### 2.2 메뉴바 메서드

```python
def create_menu_bar(self):
    """메뉴바 생성"""
    menubar = tk.Menu(self.root)
    self.root.config(menu=menubar)
    
    # 파일 메뉴
    file_menu = tk.Menu(menubar, tearoff=0)
    menubar.add_cascade(label="파일", menu=file_menu)
    file_menu.add_command(label="직원 정보 파일 열기...", 
                         command=self.menu_open_employee_file,
                         accelerator="Ctrl+O")
    file_menu.add_command(label="출력 폴더 열기...",
                         command=self.menu_open_output_folder,
                         accelerator="Ctrl+Shift+O")
    file_menu.add_separator()
    file_menu.add_command(label="종료",
                         command=self.menu_quit,
                         accelerator="Ctrl+Q")
    
    # 설정 메뉴
    settings_menu = tk.Menu(menubar, tearoff=0)
    menubar.add_cascade(label="설정", menu=settings_menu)
    settings_menu.add_command(label="설정 초기화...",
                             command=self.menu_reset_settings)
    settings_menu.add_command(label="첫 실행 상태로 되돌리기...",
                             command=self.menu_reset_first_run)
    settings_menu.add_separator()
    settings_menu.add_command(label="출력 폴더 설정...",
                             command=self.select_output_folder)
    
    # 도움말 메뉴
    help_menu = tk.Menu(menubar, tearoff=0)
    menubar.add_cascade(label="도움말", menu=help_menu)
    help_menu.add_command(label="사용자 매뉴얼",
                         command=self.menu_open_user_manual,
                         accelerator="F1")
    help_menu.add_command(label="샘플 파일 가이드",
                         command=self.menu_open_sample_guide)
    help_menu.add_separator()
    help_menu.add_command(label="정보...",
                         command=self.menu_show_about,
                         accelerator="Ctrl+I")
    
    # 키보드 단축키 바인딩
    self.bind_shortcuts()
```

#### 2.3 메뉴 핸들러 메서드

```python
def menu_open_employee_file(self):
    """메뉴: 직원 정보 파일 열기"""
    # 대시보드 탭으로 전환
    self.notebook.select(0)
    # 파일 선택 다이얼로그 표시
    self.load_file_for_dashboard()

def menu_open_output_folder(self):
    """메뉴: 출력 폴더 열기"""
    self.select_output_folder()

def menu_quit(self):
    """메뉴: 종료"""
    if messagebox.askokcancel("종료", "프로그램을 종료하시겠습니까?"):
        self.root.quit()

def menu_reset_settings(self):
    """메뉴: 설정 초기화"""
    if messagebox.askyesno("설정 초기화", 
                          "모든 설정을 초기화하시겠습니까?\n"
                          "저장된 파일 경로와 설정이 삭제됩니다."):
        # 설정 파일 삭제
        settings_file = self.settings_manager.settings_file
        if settings_file.exists():
            settings_file.unlink()
        # SettingsManager 재초기화
        self.settings_manager = SettingsManager()
        messagebox.showinfo("완료", "설정이 초기화되었습니다.")

def menu_reset_first_run(self):
    """메뉴: 첫 실행 상태로 되돌리기"""
    if messagebox.askyesno("첫 실행 상태로 되돌리기",
                          "다음 실행 시 파일 선택 다이얼로그가 표시됩니다."):
        self.settings_manager.settings['is_first_run'] = True
        self.settings_manager.save_settings()
        messagebox.showinfo("완료", "다음 실행 시 첫 실행 상태로 시작됩니다.")

def menu_open_user_manual(self):
    """메뉴: 사용자 매뉴얼 열기"""
    manual_path = os.path.join(
        os.path.dirname(__file__),
        'payroll_generator', 'docs', '사용자_매뉴얼.md'
    )
    if os.path.exists(manual_path):
        self.open_file(manual_path)
    else:
        messagebox.showwarning("파일 없음", "사용자 매뉴얼 파일을 찾을 수 없습니다.")

def menu_open_sample_guide(self):
    """메뉴: 샘플 파일 가이드 열기"""
    guide_path = os.path.join(
        os.path.dirname(__file__),
        'payroll_generator', 'docs', '샘플_파일_가이드.md'
    )
    if os.path.exists(guide_path):
        self.open_file(guide_path)
    else:
        messagebox.showwarning("파일 없음", "샘플 파일 가이드 파일을 찾을 수 없습니다.")

def menu_show_about(self):
    """메뉴: 정보 표시"""
    about_text = """급여명세서 자동생성기 v1.0

급여명세서를 자동으로 생성하는 프로그램입니다.

주요 기능:
• 직원 정보 대시보드
• 급여명세서 자동 생성 (Excel/PDF)
• 4대보험 자동 계산
• 소득세/지방소득세 자동 계산

개발: 2025
라이선스: MIT"""
    messagebox.showinfo("정보", about_text)

def open_file(self, file_path):
    """파일 열기 (시스템 기본 프로그램)"""
    try:
        if platform.system() == 'Windows':
            os.startfile(file_path)
        elif platform.system() == 'Darwin':  # macOS
            subprocess.Popen(['open', file_path])
        else:  # Linux
            subprocess.Popen(['xdg-open', file_path])
    except Exception as e:
        messagebox.showerror("오류", f"파일을 열 수 없습니다:\n{str(e)}")

def bind_shortcuts(self):
    """키보드 단축키 바인딩"""
    # Ctrl+O: 직원 정보 파일 열기
    self.root.bind('<Control-o>', lambda e: self.menu_open_employee_file())
    self.root.bind('<Command-o>', lambda e: self.menu_open_employee_file())  # macOS
    
    # Ctrl+Shift+O: 출력 폴더 열기
    self.root.bind('<Control-Shift-O>', lambda e: self.menu_open_output_folder())
    self.root.bind('<Command-Shift-O>', lambda e: self.menu_open_output_folder())  # macOS
    
    # Ctrl+Q: 종료
    self.root.bind('<Control-q>', lambda e: self.menu_quit())
    self.root.bind('<Command-q>', lambda e: self.menu_quit())  # macOS
    
    # F1: 사용자 매뉴얼
    self.root.bind('<F1>', lambda e: self.menu_open_user_manual())
    
    # Ctrl+I: 정보
    self.root.bind('<Control-i>', lambda e: self.menu_show_about())
    self.root.bind('<Command-i>', lambda e: self.menu_show_about())  # macOS
```

---

## 🔄 구현 단계

### 단계 1: 메뉴바 기본 구조 생성
**예상 시간**: 30분

**작업 내용**:
- [ ] `create_menu_bar()` 메서드 생성
- [ ] 파일, 설정, 도움말 메뉴 기본 구조 생성
- [ ] 메뉴바를 root에 연결

### 단계 2: 메뉴 핸들러 메서드 구현
**예상 시간**: 1시간

**작업 내용**:
- [ ] `menu_open_employee_file()` 구현
- [ ] `menu_open_output_folder()` 구현
- [ ] `menu_quit()` 구현
- [ ] `menu_reset_settings()` 구현
- [ ] `menu_reset_first_run()` 구현
- [ ] `menu_open_user_manual()` 구현
- [ ] `menu_open_sample_guide()` 구현
- [ ] `menu_show_about()` 구현
- [ ] `open_file()` 유틸리티 메서드 구현

### 단계 3: 키보드 단축키 바인딩
**예상 시간**: 20분

**작업 내용**:
- [ ] `bind_shortcuts()` 메서드 구현
- [ ] Windows/Linux 단축키 바인딩 (Ctrl+...)
- [ ] macOS 단축키 바인딩 (Cmd+...)
- [ ] F1 키 바인딩

### 단계 4: SettingsManager 확장 (필요시)
**예상 시간**: 20분

**작업 내용**:
- [ ] 설정 초기화 메서드 추가 (선택사항)
- [ ] 첫 실행 상태 리셋 메서드 추가 (선택사항)

### 단계 5: 테스트
**예상 시간**: 30분

**테스트 항목**:
- [ ] 메뉴바 표시 확인
- [ ] 각 메뉴 항목 클릭 동작 확인
- [ ] 키보드 단축키 동작 확인
- [ ] 설정 초기화 동작 확인
- [ ] 파일 열기 동작 확인 (매뉴얼, 가이드)
- [ ] 정보 다이얼로그 표시 확인

### 단계 6: 에러 처리 및 예외 상황
**예상 시간**: 20분

**작업 내용**:
- [ ] 파일이 없을 때 처리
- [ ] 권한 오류 처리
- [ ] 플랫폼별 차이 처리 (Windows/macOS/Linux)

---

## 📝 구현 상세

### 메뉴바 스타일

**macOS 고려사항**:
- macOS에서는 메뉴바가 화면 상단에 표시됨
- 첫 번째 메뉴는 애플 메뉴로 표시되지 않도록 주의
- `tearoff=0`으로 설정하여 메뉴 분리 방지

**Windows/Linux 고려사항**:
- 메뉴바가 창 상단에 표시됨
- 단축키 표시가 메뉴 항목 옆에 표시됨

### 키보드 단축키

| 기능 | Windows/Linux | macOS |
|------|---------------|-------|
| 직원 정보 파일 열기 | `Ctrl+O` | `Cmd+O` |
| 출력 폴더 열기 | `Ctrl+Shift+O` | `Cmd+Shift+O` |
| 종료 | `Ctrl+Q` | `Cmd+Q` |
| 사용자 매뉴얼 | `F1` | `F1` |
| 정보 | `Ctrl+I` | `Cmd+I` |

### 메뉴 항목 활성화/비활성화

향후 확장 가능성:
- 파일이 로드되지 않았을 때 일부 메뉴 비활성화
- 처리 중일 때 메뉴 비활성화

---

## ⚠️ 주의사항

### 1. 플랫폼별 차이
- macOS: 메뉴바가 화면 상단에 표시
- Windows/Linux: 메뉴바가 창 상단에 표시
- 단축키 바인딩 시 플랫폼별 처리 필요

### 2. 기존 기능과의 통합
- 기존 버튼 기능과 메뉴 기능이 동일하게 동작하도록
- 중복 코드 방지 (메서드 재사용)

### 3. 사용자 경험
- 메뉴 항목에 단축키 표시
- 확인 다이얼로그 제공 (설정 초기화, 종료 등)
- 명확한 메뉴 항목 레이블

### 4. 에러 처리
- 파일이 없을 때 적절한 메시지 표시
- 권한 오류 처리
- 플랫폼별 파일 열기 오류 처리

---

## 📊 예상 작업 시간

| 단계 | 작업 내용 | 예상 시간 |
|------|----------|----------|
| 1 | 메뉴바 기본 구조 생성 | 30분 |
| 2 | 메뉴 핸들러 메서드 구현 | 1시간 |
| 3 | 키보드 단축키 바인딩 | 20분 |
| 4 | SettingsManager 확장 (필요시) | 20분 |
| 5 | 테스트 | 30분 |
| 6 | 에러 처리 및 예외 상황 | 20분 |
| **총계** | | **2시간 40분** |

---

## ✅ 체크리스트

### 구현 전
- [ ] 현재 상태 분석 완료
- [ ] 구현 계획 작성 완료

### 구현 중
- [ ] 메뉴바 기본 구조 생성
- [ ] 메뉴 핸들러 메서드 구현
- [ ] 키보드 단축키 바인딩
- [ ] SettingsManager 확장 (필요시)
- [ ] 테스트
- [ ] 에러 처리

### 구현 후
- [ ] 문서 업데이트
- [ ] 사용자 매뉴얼 업데이트 (메뉴바 사용법 추가)

---

## 🔗 관련 파일

- `main.py` (수정 예정)
- `payroll_generator/settings.py` (확장 가능)
- `payroll_generator/docs/사용자_매뉴얼.md` (업데이트 예정)

---

## 🚀 향후 개선 사항

1. **컨텍스트 메뉴**
   - 우클릭 메뉴 추가
   - 파일 목록에서 우클릭 시 컨텍스트 메뉴

2. **최근 파일 목록**
   - 파일 메뉴에 최근 파일 목록 추가
   - 최근 5개 파일 표시

3. **메뉴 항목 활성화/비활성화**
   - 상태에 따른 메뉴 항목 활성화/비활성화
   - 처리 중일 때 메뉴 비활성화

4. **사용자 정의 단축키**
   - 사용자가 단축키를 변경할 수 있는 기능

---

**마지막 업데이트**: 2025-11-11

