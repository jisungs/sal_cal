# 💼 급여명세서 자동생성기 - 7일 완성 실행 계획

## 📋 프로젝트 개요

**목표**: 일주일 안에 판매 가능한 MVP(Minimum Viable Product) 개발 및 출시  
**타겟 고객**: 10인 이하 소상공인, 프리랜서 고용주, 소규모 학원/과외 운영자  
**가격 전략**: 오픈 기념 29,900원 → 정가 39,000원  
**예상 개발 시간**: 56시간 (1일 8시간 기준)

---

## 🎯 핵심 전략: 범위 제한으로 빠른 출시

### ✅ MVP에 포함할 기능 (Must Have)
- **대시보드 기능** (메인 첫 페이지)
  - 현재 직원 수 표시
  - 근무현황 요약
  - 월 나갈 임금 총액
  - 상황 및 특이사항
  - 통계 그래프 (직원별 급여 분포, 공제 항목 비율 등)
- 기본급 + 수당(연장근무, 상여금) 계산
- 4대보험 자동 계산 (국민연금, 건강보험, 장기요양, 고용보험)
- 간이세액표 기반 소득세/지방소득세 계산
- 엑셀 입력 → 엑셀/PDF 출력
- GUI 인터페이스 (tkinter, 탭 구조)
- 엑셀 템플릿 3종 제공

### ❌ 제외할 기능 (v2.0에서)
- 연말정산 기능
- 클라우드 동기화
- 다국어 지원
- 복잡한 변동 수당 계산
- 이메일 자동 발송 (v1.5에서 추가 가능)

---

## 📅 Day 0: 개발 환경 세팅 (사전 준비)

### 필수 작업 (2시간)
```bash
# 1. 가상환경 생성
python -m venv venv
source venv/bin/activate  # Windows: venv\Scripts\activate

# 2. 필수 패키지 설치
pip install openpyxl pandas reportlab pyinstaller pillow matplotlib
# matplotlib: 대시보드 그래프 시각화용

# 3. 프로젝트 구조 생성
mkdir payroll_generator
cd payroll_generator
mkdir templates data docs output logs
git init
```

### 🔴 MVP 필수 개선 사항 (종합 분석 반영)

**참고**: 이 섹션은 전문가 검증 결과를 반영한 필수 개선 사항입니다.

#### 보안 및 안정성
- [x] 주민번호 마스킹 처리 (PDF 출력 시) ✅
- [x] 로깅 시스템 추가 (`logs/app.log`) ✅
- [x] 파일 경로 정규화 및 검증 ✅

#### 사용자 경험
- [x] **대시보드 기능** (메인 첫 페이지) ✅
  - [x] 현재 직원 수, 총 급여, 총 공제, 총 실수령액 표시 ✅
  - [x] **월급여 정산현황** 막대 그래프 (직원별 실수령액) ✅
  - [x] **근무자구성** 도넛 그래프 (정규직/계약직/신입 비율) ✅
  - [x] 특이사항 표시 (연장근무, 상여금 등) ✅
- [x] 엑셀 파일 미리보기 기능 (첫 3행) ✅
- [ ] 완료 후 "파일 열기" 버튼 추가 (대시보드에는 있으나 메인 GUI 통합 필요)
- [ ] 인라인 오류 메시지 표시 (대시보드에는 있으나 메인 GUI 통합 필요)
- [ ] 부드러운 오류 메시지 문구 적용 (대시보드에는 있으나 메인 GUI 통합 필요)

### 프로젝트 구조
```
payroll_generator/
├── main.py              # GUI 메인 진입점
├── calculator.py        # 급여 계산 로직
├── excel_handler.py     # 엑셀 입출력 처리
├── pdf_generator.py     # PDF 생성
├── dashboard.py         # 대시보드 데이터 수집 및 시각화
├── config.py            # 설정값 (세율, 상한액 등)
├── utils.py             # 유틸리티 함수 (보안, 파일 경로 정규화)
├── logger.py            # 로깅 시스템 설정
├── requirements.txt     # 패키지 목록
├── templates/           # 엑셀 템플릿
│   ├── employee_template.xlsx
│   ├── payroll_template_basic.xlsx
│   └── payroll_template_detailed.xlsx
├── data/                # 샘플 데이터
│   └── sample_employees.xlsx
├── docs/                # 문서
│   └── manual.md
├── output/              # 생성된 파일 저장
├── logs/                # 로그 파일 저장
│   └── app.log
└── assets/              # 아이콘, 폰트 등
    └── NanumGothic.ttf
```

---

## 📋 Day 1: 요구사항 정의 및 설계 (8시간)

### 오전 (4시간): 시장 조사 및 요구사항 정의

#### 1. 타겟 고객 명확화
- **주요 타겟**: 10인 이하 소상공인, 프리랜서 고용주
- **보조 타겟**: 학원/과외 운영자, 카페/식당 사장님
- **고객 인터뷰**: 최소 3명과 간단한 인터뷰 (30분씩)

#### 2. 경쟁 제품 분석
- 크몽/오픈마켓에서 "급여명세서", "급여 자동화" 키워드로 검색
- 가격대: 15,000원 ~ 99,000원
- 기능 비교표 작성
- 차별화 포인트 도출

#### 3. 가격 책정
- **오픈 기념가**: 29,900원 (첫 50명)
- **정가**: 39,000원
- **경쟁력**: 중간 가격대, 기능 대비 합리적

#### 4. 핵심 기능 우선순위 결정
```
P0 (필수): 기본급 계산, 4대보험, 소득세, 엑셀 출력
P1 (중요): PDF 출력, GUI 인터페이스
P2 (선택): 템플릿 3종, 샘플 데이터
```

### 오후 (4시간): 데이터 구조 설계 및 화면 설계

#### 1. 데이터 구조 설계

**직원 정보 (입력 엑셀)**
| 컬럼명 | 타입 | 필수 | 설명 |
|--------|------|------|------|
| 이름 | string | ✅ | 직원 이름 |
| 주민번호 | string | ✅ | 주민번호 앞자리 (세금계산용) |
| 입사일 | date | ✅ | YYYY-MM-DD 형식 |
| 계좌번호 | string | ❌ | 급여 이체용 |
| 기본급 | integer | ✅ | 월 기본급 |
| 연장근무시간 | integer | ❌ | 시간당 단가 별도 입력 |
| 연장근무단가 | integer | ❌ | 기본급/일근무시간 * 1.5 권장 |
| 상여금 | integer | ❌ | 상여금 금액 |
| 공제사항 | string | ❌ | 기타 공제 메모 |

**급여명세서 출력 구조**
- 지급 항목: 기본급, 연장근무수당, 상여금, 총 지급액
- 공제 항목: 국민연금, 건강보험, 장기요양, 고용보험, 소득세, 지방소득세, 총 공제액
- 실수령액: 총 지급액 - 총 공제액

#### 2. 화면 설계 (와이어프레임)

**메인 화면 구성 (탭 구조)**

**탭 1: 대시보드 (첫 화면)**
```
┌─────────────────────────────────────────┐
│  급여명세서 자동생성기 v1.0            │
├─────────────────────────────────────────┤
│ [대시보드] [급여명세서 생성]            │
├─────────────────────────────────────────┤
│                                         │
│  📊 대시보드                            │
│                                         │
│  ┌──────────┬──────────┬──────────┐    │
│  │ 직원 수  │ 총 급여  │ 총 공제  │    │
│  │   10명   │ 35,000원 │ 8,500원  │    │
│  └──────────┴──────────┴──────────┘    │
│                                         │
│  ┌──────────────────┬─────────────────┐│
│  │ 📈 월급여 정산현황│ 📊 근무자구성   ││
│  │   (막대 그래프)   │   (도넛 그래프) ││
│  │ ┌──────────────┐ │ ┌─────────────┐ ││
│  │ │ [막대 그래프] │ │ │ [도넛 그래프]│ ││
│  │ │ 직원별 급여  │ │ │ 정규직/계약직│ ││
│  │ │ 정산 현황    │ │ │ /신입 비율  │ ││
│  │ └──────────────┘ │ └─────────────┘ ││
│  └──────────────────┴─────────────────┘│
│                                         │
│  ⚠️ 특이사항                            │
│  - 이번 달 연장근무 증가: 3명           │
│  - 상여금 지급: 2명                     │
│                                         │
│  [엑셀 파일 불러오기]                   │
│                                         │
└─────────────────────────────────────────┘
```

**탭 2: 급여명세서 생성**
```
┌─────────────────────────────────────────┐
│  급여명세서 자동생성기 v1.0            │
├─────────────────────────────────────────┤
│ [대시보드] [급여명세서 생성]            │
├─────────────────────────────────────────┤
│                                         │
│  [📁 직원 정보 엑셀 파일 선택]         │
│  파일 경로: [________________] [찾아보기]│
│  ⚠️ 인라인 오류 메시지 표시 영역       │
│                                         │
│  [파일 미리보기]                        │
│  ┌─────────────────────────────────┐   │
│  │ 이름  │ 주민번호  │ 입사일 │ 기본급│   │
│  │ 홍길동│ 123456-***│ 2024-01│ 3,000│   │
│  └─────────────────────────────────┘   │
│                                         │
│  생성 기간: [2025-01] [▼]              │
│                                         │
│  출력 형식: ○ 엑셀  ○ PDF  ○ 둘 다    │
│                                         │
│  출력 폴더: [________________] [찾아보기]│
│                                         │
│  [생성하기]  [초기화]                   │
│                                         │
│  진행 상태: [████████░░] 80%           │
│  처리 중: 홍길동 (3/10)                │
│                                         │
│  [📁 출력 폴더 열기] (완료 후 활성화)   │
│                                         │
└─────────────────────────────────────────┘
```

#### 3. 엑셀 템플릿 기본 구조 작성
- `templates/employee_template.xlsx` 생성
- 필수 컬럼 헤더 작성
- 샘플 데이터 3행 입력

---

## 💻 Day 2: 기술 스택 확정 및 프로젝트 초기화 (8시간)

### 오전 (4시간): 핵심 모듈 개발 시작

#### 1. logger.py 작성 (로깅 시스템)
```python
# logger.py
import logging
import os

def setup_logger():
    """로깅 시스템 초기화"""
    log_dir = os.path.join(os.path.dirname(__file__), 'logs')
    os.makedirs(log_dir, exist_ok=True)
    
    log_file = os.path.join(log_dir, 'app.log')
    
    logging.basicConfig(
        level=logging.INFO,
        format='%(asctime)s - %(name)s - %(levelname)s - %(message)s',
        handlers=[
            logging.FileHandler(log_file, encoding='utf-8'),
            logging.StreamHandler()
        ]
    )
    
    return logging.getLogger(__name__)
```

#### 2. utils.py 작성 (보안 및 유틸리티 함수)
```python
# utils.py
import os
import re

def mask_resident_number(resident_number):
    """주민번호 마스킹 처리 (앞 6자리만 표시, 나머지 마스킹)"""
    if not resident_number:
        return ""
    
    # 숫자만 추출
    numbers = re.sub(r'[^0-9]', '', str(resident_number))
    
    if len(numbers) >= 7:
        return f"{numbers[:6]}-*******"
    elif len(numbers) == 6:
        return f"{numbers}-*******"
    else:
        return "*******"

def normalize_path(file_path):
    """파일 경로 정규화 및 검증"""
    if not file_path:
        raise ValueError("파일 경로가 비어있습니다.")
    
    # 절대 경로로 변환
    abs_path = os.path.abspath(file_path)
    
    # 경로 정규화
    normalized = os.path.normpath(abs_path)
    
    # 경로 검증 (상위 디렉토리 접근 방지)
    if '..' in normalized:
        raise ValueError("잘못된 파일 경로입니다.")
    
    return normalized

def validate_file_path(file_path, allowed_extensions=None):
    """파일 경로 유효성 검증"""
    normalized = normalize_path(file_path)
    
    if not os.path.exists(normalized):
        raise FileNotFoundError(f"파일을 찾을 수 없습니다: {normalized}")
    
    if allowed_extensions:
        ext = os.path.splitext(normalized)[1].lower()
        if ext not in allowed_extensions:
            raise ValueError(f"지원하지 않는 파일 형식입니다: {ext}")
    
    return normalized
```

#### 3. config.py 작성
```python
# config.py
# 4대보험 요율 및 상한액 설정 (2025년 기준)

INSURANCE_RATES = {
    'national_pension': 0.045,      # 국민연금 4.5%
    'health_insurance': 0.03545,    # 건강보험 3.545%
    'long_term_care': 0.004591,     # 장기요양 0.4591% (건강보험의 12.95%)
    'employment_insurance': 0.009,  # 고용보험 0.9%
}

INSURANCE_LIMITS = {
    'national_pension': 5_530_000,   # 국민연금 상한액
    'health_insurance': 5_530_000,  # 건강보험 상한액
    'employment_insurance': 5_530_000, # 고용보험 상한액
}

# 소득세 간이세액표 (구간별)
INCOME_TAX_TABLE = [
    # (과세표준 시작, 과세표준 끝, 세율, 누진공제)
    (0, 1_200_000, 0.06, 0),
    (1_200_000, 4_600_000, 0.15, 108_000),
    (4_600_000, 8_800_000, 0.24, 522_000),
    (8_800_000, 15_000_000, 0.35, 1_490_000),
    (15_000_000, float('inf'), 0.38, 1_940_000),
]

LOCAL_TAX_RATE = 0.10  # 지방소득세 (소득세의 10%)
```

#### 2. calculator.py 기본 구조 작성
```python
# calculator.py
from config import INSURANCE_RATES, INSURANCE_LIMITS, INCOME_TAX_TABLE, LOCAL_TAX_RATE

class PayrollCalculator:
    def __init__(self):
        self.rates = INSURANCE_RATES
        self.limits = INSURANCE_LIMITS
    
    def calculate_insurance(self, base_salary, insurance_type):
        """4대보험 계산"""
        # 상한액 적용
        taxable_amount = min(base_salary, self.limits[insurance_type])
        return int(taxable_amount * self.rates[insurance_type])
    
    def calculate_income_tax(self, taxable_income):
        """소득세 계산 (간이세액표 기반)"""
        # 간이세액표 적용 로직
        pass
    
    def calculate_deductions(self, employee_data):
        """전체 공제액 계산"""
        pass
    
    def calculate_net_pay(self, employee_data):
        """실수령액 계산"""
        pass
```

### 오후 (4시간): 엑셀 처리 모듈 및 대시보드 모듈 개발

#### 1. dashboard.py 기본 구조 작성
```python
# dashboard.py
import pandas as pd
import matplotlib.pyplot as plt
from matplotlib.backends.backend_tkagg import FigureCanvasTkAgg
from calculator import PayrollCalculator
from logger import setup_logger

logger = setup_logger()

class Dashboard:
    def __init__(self):
        self.calculator = PayrollCalculator()
    
    def analyze_employee_data(self, df):
        """직원 데이터 분석 및 대시보드 데이터 생성"""
        try:
            total_employees = len(df)
            
            # 급여 계산
            total_payment = 0
            total_deduction = 0
            total_net_pay = 0
            
            payroll_summary = []
            for idx, row in df.iterrows():
                payroll_data = self.calculator.calculate_deductions(row)
                total_payment += payroll_data['총지급액']
                total_deduction += payroll_data['총공제액']
                total_net_pay += payroll_data['실수령액']
                
                payroll_summary.append({
                    '이름': row['이름'],
                    '총지급액': payroll_data['총지급액'],
                    '실수령액': payroll_data['실수령액']
                })
            
            # 근무현황 분석
            work_status = self._analyze_work_status(df)
            
            # 특이사항 분석
            special_notes = self._analyze_special_notes(df)
            
            return {
                'total_employees': total_employees,
                'total_payment': total_payment,
                'total_deduction': total_deduction,
                'total_net_pay': total_net_pay,
                'payroll_summary': payroll_summary,
                'work_status': work_status,
                'special_notes': special_notes
            }
        except Exception as e:
            logger.exception(f"대시보드 데이터 분석 오류: {str(e)}")
            raise
    
    def _analyze_work_status(self, df):
        """근무현황 분석"""
        # 입사일 기준으로 신입/기존직원 구분
        # 실제 구현 시 더 세밀한 로직 필요
        return {
            'regular': len(df),  # 임시값
            'contract': 0,
            'new': 0
        }
    
    def _analyze_special_notes(self, df):
        """특이사항 분석"""
        notes = []
        
        # 연장근무 있는 직원 수
        overtime_count = df[df.get('연장근무시간', 0) > 0].shape[0]
        if overtime_count > 0:
            notes.append(f"이번 달 연장근무: {overtime_count}명")
        
        # 상여금 지급 직원 수
        bonus_count = df[df.get('상여금', 0) > 0].shape[0]
        if bonus_count > 0:
            notes.append(f"상여금 지급: {bonus_count}명")
        
        return notes
    
    def create_salary_chart(self, payroll_summary):
        """월급여 정산현황 막대 그래프 생성"""
        try:
            fig, ax = plt.subplots(figsize=(8, 4))
            
            names = [item['이름'] for item in payroll_summary]
            salaries = [item['실수령액'] for item in payroll_summary]
            
            ax.bar(names, salaries, color='#304FFE')
            ax.set_xlabel('직원', fontsize=10)
            ax.set_ylabel('실수령액 (원)', fontsize=10)
            ax.set_title('월급여 정산현황', fontsize=12, fontweight='bold')
            ax.tick_params(axis='x', rotation=45)
            
            # 값 표시
            for i, v in enumerate(salaries):
                ax.text(i, v, f'{v:,}', ha='center', va='bottom', fontsize=8)
            
            plt.tight_layout()
            return fig
        except Exception as e:
            logger.exception(f"급여 그래프 생성 오류: {str(e)}")
            raise
    
    def create_workforce_chart(self, work_status):
        """근무자구성 도넛 그래프 생성"""
        try:
            fig, ax = plt.subplots(figsize=(6, 6))
            
            labels = []
            sizes = []
            colors = ['#304FFE', '#FFD54F', '#4CAF50']
            
            if work_status.get('regular', 0) > 0:
                labels.append('정규직')
                sizes.append(work_status['regular'])
            if work_status.get('contract', 0) > 0:
                labels.append('계약직')
                sizes.append(work_status['contract'])
            if work_status.get('new', 0) > 0:
                labels.append('신입')
                sizes.append(work_status['new'])
            
            if not sizes:
                # 데이터가 없을 경우
                ax.text(0.5, 0.5, '데이터 없음', 
                       ha='center', va='center', fontsize=12)
                ax.set_title('근무자구성', fontsize=12, fontweight='bold')
                plt.tight_layout()
                return fig
            
            # 도넛 그래프 생성 (wedgeprops로 중앙 구멍 만들기)
            wedges, texts, autotexts = ax.pie(
                sizes, 
                labels=labels, 
                autopct='%1.1f%%',
                startangle=90,
                colors=colors[:len(labels)],
                wedgeprops=dict(width=0.5),  # 도넛 모양
                textprops={'fontsize': 10}
            )
            
            ax.set_title('근무자구성', fontsize=12, fontweight='bold', pad=20)
            
            plt.tight_layout()
            return fig
        except Exception as e:
            logger.exception(f"근무자구성 그래프 생성 오류: {str(e)}")
            raise
```

#### 2. excel_handler.py 작성
```python
# excel_handler.py
import pandas as pd
import openpyxl
from openpyxl.styles import Font, Alignment, PatternFill, Border, Side
from utils import normalize_path, validate_file_path
from logger import setup_logger

logger = setup_logger()

class ExcelHandler:
    def __init__(self):
        self.required_columns = ['이름', '주민번호', '입사일', '기본급']
    
    def read_employee_data(self, file_path):
        """직원 정보 엑셀 읽기"""
        try:
            # 파일 경로 정규화 및 검증
            validated_path = validate_file_path(file_path, ['.xlsx', '.xls'])
            logger.info(f"엑셀 파일 읽기 시작: {validated_path}")
            
            df = pd.read_excel(validated_path)
            self.validate_data(df)
            logger.info(f"엑셀 파일 읽기 완료: {len(df)}행")
            return df
        except Exception as e:
            logger.exception(f"엑셀 파일 읽기 오류: {str(e)}")
            raise ValueError(f"엑셀 파일 읽기 오류: {str(e)}")
    
    def validate_data(self, df):
        """필수 컬럼 검증"""
        missing_cols = [col for col in self.required_columns if col not in df.columns]
        if missing_cols:
            error_msg = f"필수 컬럼이 없습니다: {', '.join(missing_cols)}"
            logger.error(error_msg)
            raise ValueError(error_msg)
    
    def get_preview(self, file_path, num_rows=3):
        """엑셀 파일 미리보기 (첫 N행 반환)"""
        try:
            validated_path = validate_file_path(file_path, ['.xlsx', '.xls'])
            df = pd.read_excel(validated_path, nrows=num_rows)
            return df.to_dict('records')
        except Exception as e:
            logger.exception(f"미리보기 오류: {str(e)}")
            raise
    
    def write_payroll(self, payroll_data, output_path, template_path=None):
        """급여명세서 엑셀 생성"""
        try:
            output_path = normalize_path(output_path)
            # 템플릿 사용 또는 새로 생성
            logger.info(f"급여명세서 엑셀 생성: {output_path}")
        except Exception as e:
            logger.exception(f"엑셀 생성 오류: {str(e)}")
            raise
```

#### 2. Git 저장소 초기 설정
```bash
# .gitignore 작성
echo "venv/
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
output/
*.xlsx
*.pdf
.DS_Store" > .gitignore

git add .
git commit -m "Initial project setup"
```

---

## 💻 Day 3: 급여 계산 로직 완성 (8시간)

### 오전 (4시간): 계산 엔진 완성

#### 1. calculator.py 완성
- 4대보험 계산 로직 구현
- 소득세 간이세액표 적용
- 지방소득세 계산
- 총 공제액 및 실수령액 계산

**핵심 로직 예시**
```python
def calculate_deductions(self, employee_data):
    base_salary = employee_data['기본급']
    overtime_pay = employee_data.get('연장근무수당', 0)
    bonus = employee_data.get('상여금', 0)
    
    total_payment = base_salary + overtime_pay + bonus
    
    # 4대보험 계산 (기본급 기준)
    national_pension = self.calculate_insurance(base_salary, 'national_pension')
    health_insurance = self.calculate_insurance(base_salary, 'health_insurance')
    long_term_care = int(health_insurance * 0.1295)  # 건강보험의 12.95%
    employment_insurance = self.calculate_insurance(base_salary, 'employment_insurance')
    
    # 소득세 계산 (과세표준 = 총 지급액 - 4대보험)
    taxable_income = total_payment - (national_pension + health_insurance + 
                                      long_term_care + employment_insurance)
    income_tax = self.calculate_income_tax(taxable_income)
    local_tax = int(income_tax * LOCAL_TAX_RATE)
    
    total_deduction = (national_pension + health_insurance + long_term_care + 
                      employment_insurance + income_tax + local_tax)
    
    return {
        '총지급액': total_payment,
        '국민연금': national_pension,
        '건강보험': health_insurance,
        '장기요양': long_term_care,
        '고용보험': employment_insurance,
        '소득세': income_tax,
        '지방소득세': local_tax,
        '총공제액': total_deduction,
        '실수령액': total_payment - total_deduction
    }
```

#### 2. 단위 테스트 작성
- 기본급만 있는 경우
- 연장근무수당 포함 경우
- 상여금 포함 경우
- 상한액 초과 경우

### 오후 (4시간): 엑셀 출력 기능 완성

#### 1. excel_handler.py 완성
- 급여명세서 엑셀 템플릿 적용
- 스타일링 (폰트, 색상, 테두리)
- 직원별 시트 생성 또는 개별 파일 생성

#### 2. 통합 테스트
- 샘플 데이터로 전체 프로세스 테스트
- 출력 파일 검증

---

## 💻 Day 4: GUI 개발 및 통합 (8시간)

### 오전 (4시간): tkinter GUI 개발 (탭 구조 + 대시보드)

#### 1. main.py 작성 (탭 구조로 변경)
```python
# main.py
import tkinter as tk
from tkinter import ttk, filedialog, messagebox
from calculator import PayrollCalculator
from excel_handler import ExcelHandler
from pdf_generator import PDFGenerator
from dashboard import Dashboard
from logger import setup_logger
from matplotlib.backends.backend_tkagg import FigureCanvasTkAgg
import threading
import os
import subprocess
import platform

logger = setup_logger()

class PayrollApp:
    def __init__(self, root):
        self.root = root
        self.root.title("급여명세서 자동생성기 v1.0")
        self.root.geometry("900x750")
        self.root.resizable(False, False)
        
        self.calculator = PayrollCalculator()
        self.excel_handler = ExcelHandler()
        self.pdf_generator = PDFGenerator()
        self.dashboard = Dashboard()
        
        self.employee_file_path = tk.StringVar()
        self.output_folder_path = tk.StringVar(value="./output")
        self.period = tk.StringVar(value="2025-01")
        self.output_format = tk.StringVar(value="both")
        self.generated_files = []
        self.current_df = None  # 현재 로드된 직원 데이터
        
        self.create_widgets()
        logger.info("애플리케이션 시작")
    
    def create_widgets(self):
        # 탭 노트북 생성
        self.notebook = ttk.Notebook(self.root)
        self.notebook.pack(fill=tk.BOTH, expand=True, padx=10, pady=10)
        
        # 탭 1: 대시보드
        self.dashboard_frame = ttk.Frame(self.notebook)
        self.notebook.add(self.dashboard_frame, text="📊 대시보드")
        self.create_dashboard_tab()
        
        # 탭 2: 급여명세서 생성
        self.payroll_frame = ttk.Frame(self.notebook)
        self.notebook.add(self.payroll_frame, text="📄 급여명세서 생성")
        self.create_payroll_tab()
        
        # 기본 탭을 대시보드로 설정
        self.notebook.select(0)
    
    def create_dashboard_tab(self):
        """대시보드 탭 UI 생성"""
        # 상단: 통계 카드
        stats_frame = ttk.LabelFrame(self.dashboard_frame, text="📊 주요 지표", padding=10)
        stats_frame.pack(fill=tk.X, padx=10, pady=5)
        
        self.employee_count_label = ttk.Label(stats_frame, text="직원 수: -", font=("맑은 고딕", 12, "bold"))
        self.employee_count_label.grid(row=0, column=0, padx=20, pady=5)
        
        self.total_payment_label = ttk.Label(stats_frame, text="총 급여: -", font=("맑은 고딕", 12, "bold"))
        self.total_payment_label.grid(row=0, column=1, padx=20, pady=5)
        
        self.total_deduction_label = ttk.Label(stats_frame, text="총 공제: -", font=("맑은 고딕", 12, "bold"))
        self.total_deduction_label.grid(row=0, column=2, padx=20, pady=5)
        
        self.total_net_pay_label = ttk.Label(stats_frame, text="총 실수령액: -", font=("맑은 고딕", 12, "bold"), foreground="blue")
        self.total_net_pay_label.grid(row=0, column=3, padx=20, pady=5)
        
        # 그래프 영역 (나란히 배치)
        charts_container = ttk.Frame(self.dashboard_frame)
        charts_container.pack(fill=tk.BOTH, expand=True, padx=10, pady=5)
        
        # 월급여 정산현황 (막대 그래프)
        salary_chart_frame = ttk.LabelFrame(charts_container, text="📈 월급여 정산현황", padding=10)
        salary_chart_frame.pack(side=tk.LEFT, fill=tk.BOTH, expand=True, padx=5)
        
        self.salary_chart_canvas = None  # 급여 그래프 캔버스
        
        # 근무자구성 (도넛 그래프)
        workforce_chart_frame = ttk.LabelFrame(charts_container, text="📊 근무자구성", padding=10)
        workforce_chart_frame.pack(side=tk.LEFT, fill=tk.BOTH, expand=True, padx=5)
        
        self.workforce_chart_canvas = None  # 근무자구성 그래프 캔버스
        
        # 하단: 특이사항
        notes_frame = ttk.LabelFrame(self.dashboard_frame, text="⚠️ 특이사항", padding=10)
        notes_frame.pack(fill=tk.X, padx=10, pady=5)
        
        self.notes_text = tk.Text(notes_frame, height=4, width=80)
        self.notes_text.pack(fill=tk.BOTH, expand=True)
        
        # 파일 불러오기 버튼
        load_btn_frame = ttk.Frame(self.dashboard_frame)
        load_btn_frame.pack(pady=10)
        
        ttk.Button(load_btn_frame, text="📁 엑셀 파일 불러오기", 
                  command=self.load_file_for_dashboard, width=25).pack()
    
    def load_file_for_dashboard(self):
        """대시보드용 파일 로드"""
        filename = filedialog.askopenfilename(
            title="직원 정보 엑셀 파일 선택",
            filetypes=[("Excel files", "*.xlsx *.xls"), ("All files", "*.*")]
        )
        if filename:
            try:
                self.employee_file_path.set(filename)
                self.current_df = self.excel_handler.read_employee_data(filename)
                self.update_dashboard()
                logger.info(f"대시보드 데이터 로드 완료: {len(self.current_df)}명")
            except Exception as e:
                messagebox.showerror("오류", f"파일 로드 오류:\n{str(e)}")
                logger.exception(f"대시보드 파일 로드 오류: {str(e)}")
    
    def update_dashboard(self):
        """대시보드 데이터 업데이트"""
        if self.current_df is None:
            return
        
        try:
            # 데이터 분석
            dashboard_data = self.dashboard.analyze_employee_data(self.current_df)
            
            # 통계 업데이트
            self.employee_count_label.config(text=f"직원 수: {dashboard_data['total_employees']}명")
            self.total_payment_label.config(text=f"총 급여: {dashboard_data['total_payment']:,}원")
            self.total_deduction_label.config(text=f"총 공제: {dashboard_data['total_deduction']:,}원")
            self.total_net_pay_label.config(text=f"총 실수령액: {dashboard_data['total_net_pay']:,}원")
            
            # 월급여 정산현황 그래프 업데이트 (막대 그래프)
            if self.salary_chart_canvas:
                self.salary_chart_canvas.get_tk_widget().destroy()
            
            salary_fig = self.dashboard.create_salary_chart(dashboard_data['payroll_summary'])
            # charts_container의 첫 번째 자식 (salary_chart_frame) 찾기
            charts_container = self.dashboard_frame.winfo_children()[1]
            salary_chart_frame = charts_container.winfo_children()[0]
            self.salary_chart_canvas = FigureCanvasTkAgg(salary_fig, salary_chart_frame)
            self.salary_chart_canvas.draw()
            self.salary_chart_canvas.get_tk_widget().pack(fill=tk.BOTH, expand=True)
            
            # 근무자구성 그래프 업데이트 (도넛 그래프)
            if self.workforce_chart_canvas:
                self.workforce_chart_canvas.get_tk_widget().destroy()
            
            workforce_fig = self.dashboard.create_workforce_chart(dashboard_data['work_status'])
            # charts_container의 두 번째 자식 (workforce_chart_frame) 찾기
            workforce_chart_frame = charts_container.winfo_children()[1]
            self.workforce_chart_canvas = FigureCanvasTkAgg(workforce_fig, workforce_chart_frame)
            self.workforce_chart_canvas.draw()
            self.workforce_chart_canvas.get_tk_widget().pack(fill=tk.BOTH, expand=True)
            
            # 특이사항 업데이트
            self.notes_text.delete(1.0, tk.END)
            if dashboard_data['special_notes']:
                notes_text = "\n".join(dashboard_data['special_notes'])
            else:
                notes_text = "특이사항 없음"
            self.notes_text.insert(1.0, notes_text)
            
        except Exception as e:
            messagebox.showerror("오류", f"대시보드 업데이트 오류:\n{str(e)}")
            logger.exception(f"대시보드 업데이트 오류: {str(e)}")
    
    def create_payroll_tab(self):
        """급여명세서 생성 탭 UI 생성"""
        # 파일 선택 프레임
        file_frame = ttk.LabelFrame(self.payroll_frame, text="입력 파일", padding=10)
        file_frame.pack(fill=tk.X, padx=10, pady=5)
        
        entry_frame = ttk.Frame(file_frame)
        entry_frame.pack(fill=tk.X)
        
        ttk.Entry(entry_frame, textvariable=self.employee_file_path, width=50).pack(side=tk.LEFT, padx=5)
        ttk.Button(entry_frame, text="찾아보기", command=self.select_employee_file).pack(side=tk.LEFT)
        
        # 인라인 오류 메시지 라벨
        self.error_label = ttk.Label(file_frame, text="", foreground="red", font=("맑은 고딕", 9))
        self.error_label.pack(pady=(5, 0))
        
        # 미리보기 프레임
        preview_frame = ttk.LabelFrame(self.payroll_frame, text="파일 미리보기", padding=10)
        preview_frame.pack(fill=tk.BOTH, expand=True, padx=10, pady=5)
        
        # 미리보기 트리뷰
        columns = ('이름', '주민번호', '입사일', '기본급')
        self.preview_tree = ttk.Treeview(preview_frame, columns=columns, show='headings', height=5)
        for col in columns:
            self.preview_tree.heading(col, text=col)
            self.preview_tree.column(col, width=120)
        self.preview_tree.pack(fill=tk.BOTH, expand=True)
        
        # 설정 프레임
        settings_frame = ttk.LabelFrame(self.payroll_frame, text="설정", padding=10)
        settings_frame.pack(fill=tk.X, padx=10, pady=5)
        
        ttk.Label(settings_frame, text="생성 기간:").grid(row=0, column=0, sticky=tk.W, pady=5)
        ttk.Entry(settings_frame, textvariable=self.period, width=15).grid(row=0, column=1, padx=5)
        
        ttk.Label(settings_frame, text="출력 형식:").grid(row=1, column=0, sticky=tk.W, pady=5)
        ttk.Radiobutton(settings_frame, text="엑셀", variable=self.output_format, value="excel").grid(row=1, column=1, padx=5)
        ttk.Radiobutton(settings_frame, text="PDF", variable=self.output_format, value="pdf").grid(row=1, column=2, padx=5)
        ttk.Radiobutton(settings_frame, text="둘 다", variable=self.output_format, value="both").grid(row=1, column=3, padx=5)
        
        # 출력 폴더 프레임
        output_frame = ttk.LabelFrame(self.payroll_frame, text="출력 폴더", padding=10)
        output_frame.pack(fill=tk.X, padx=10, pady=5)
        
        ttk.Entry(output_frame, textvariable=self.output_folder_path, width=50).pack(side=tk.LEFT, padx=5)
        ttk.Button(output_frame, text="찾아보기", command=self.select_output_folder).pack(side=tk.LEFT)
        
        # 버튼 프레임
        button_frame = ttk.Frame(self.payroll_frame)
        button_frame.pack(pady=20)
        
        ttk.Button(button_frame, text="생성하기", command=self.start_generation, width=15).pack(side=tk.LEFT, padx=5)
        ttk.Button(button_frame, text="초기화", command=self.reset_fields, width=15).pack(side=tk.LEFT, padx=5)
        
        # 진행 상태 프레임
        progress_frame = ttk.LabelFrame(self.payroll_frame, text="진행 상태", padding=10)
        progress_frame.pack(fill=tk.BOTH, expand=True, padx=10, pady=5)
        
        self.progress_var = tk.DoubleVar()
        self.progress_bar = ttk.Progressbar(progress_frame, variable=self.progress_var, maximum=100)
        self.progress_bar.pack(fill=tk.X, pady=5)
        
        self.status_label = ttk.Label(progress_frame, text="대기 중...")
        self.status_label.pack(pady=5)
        
        # 완료 후 액션 프레임
        self.action_frame = ttk.Frame(self.payroll_frame)
        self.action_frame.pack(pady=10)
        
        self.open_folder_btn = ttk.Button(
            self.action_frame, 
            text="📁 출력 폴더 열기", 
            command=self.open_output_folder,
            state=tk.DISABLED
        )
        self.open_folder_btn.pack(side=tk.LEFT, padx=5)
    
    def select_employee_file(self):
        filename = filedialog.askopenfilename(
            title="직원 정보 엑셀 파일 선택",
            filetypes=[("Excel files", "*.xlsx *.xls"), ("All files", "*.*")]
        )
        if filename:
            self.employee_file_path.set(filename)
            self.error_label.config(text="")
            self.load_preview(filename)
    
    def load_preview(self, file_path):
        """엑셀 파일 미리보기 로드"""
        try:
            # 기존 미리보기 데이터 삭제
            for item in self.preview_tree.get_children():
                self.preview_tree.delete(item)
            
            # 미리보기 데이터 로드
            preview_data = self.excel_handler.get_preview(file_path, num_rows=3)
            
            for row in preview_data:
                values = (
                    str(row.get('이름', '')),
                    str(row.get('주민번호', ''))[:6] + '-*******',  # 마스킹
                    str(row.get('입사일', '')),
                    f"{row.get('기본급', 0):,}"
                )
                self.preview_tree.insert('', tk.END, values=values)
            
            logger.info(f"미리보기 로드 완료: {len(preview_data)}행")
        except Exception as e:
            error_msg = "입력 형식에 문제가 있어요! 😊 파일을 확인해주세요."
            self.error_label.config(text=error_msg)
            logger.exception(f"미리보기 로드 오류: {str(e)}")
    
    def open_output_folder(self):
        """출력 폴더 열기"""
        folder_path = self.output_folder_path.get()
        if os.path.exists(folder_path):
            if platform.system() == 'Windows':
                os.startfile(folder_path)
            elif platform.system() == 'Darwin':  # macOS
                subprocess.Popen(['open', folder_path])
            else:  # Linux
                subprocess.Popen(['xdg-open', folder_path])
            logger.info(f"출력 폴더 열기: {folder_path}")
    
    def select_output_folder(self):
        folder = filedialog.askdirectory(title="출력 폴더 선택")
        if folder:
            self.output_folder_path.set(folder)
    
    def reset_fields(self):
        self.employee_file_path.set("")
        self.output_folder_path.set("./output")
        self.period.set("2025-01")
        self.output_format.set("both")
        self.progress_var.set(0)
        self.status_label.config(text="대기 중...")
    
    def start_generation(self):
        if not self.employee_file_path.get():
            self.error_label.config(text="직원 정보 파일을 선택해주세요! 😊")
            return
        
        # 오류 메시지 초기화
        self.error_label.config(text="")
        self.open_folder_btn.config(state=tk.DISABLED)
        self.generated_files = []
        
        # 별도 스레드에서 실행 (GUI 멈춤 방지)
        thread = threading.Thread(target=self.generate_payroll)
        thread.daemon = True
        thread.start()
    
    def generate_payroll(self):
        try:
            # 진행 상태 업데이트
            self.status_label.config(text="직원 정보 읽는 중...")
            self.progress_var.set(10)
            
            # 엑셀 파일 읽기
            df = self.excel_handler.read_employee_data(self.employee_file_path.get())
            total_employees = len(df)
            
            # 출력 폴더 생성
            output_folder = self.output_folder_path.get()
            os.makedirs(output_folder, exist_ok=True)
            
            # 각 직원별 처리
            for idx, row in df.iterrows():
                employee_name = row['이름']
                self.status_label.config(text=f"처리 중: {employee_name} ({idx+1}/{total_employees})")
                
                # 급여 계산
                payroll_data = self.calculator.calculate_deductions(row)
                
                # 출력 형식에 따라 생성
                output_format = self.output_format.get()
                if output_format in ['excel', 'both']:
                    excel_path = os.path.join(output_folder, f"{employee_name}_급여명세서.xlsx")
                    self.excel_handler.write_payroll(payroll_data, excel_path, row)
                    self.generated_files.append(excel_path)
                
                if output_format in ['pdf', 'both']:
                    pdf_path = os.path.join(output_folder, f"{employee_name}_급여명세서.pdf")
                    self.pdf_generator.generate_payslip(payroll_data, row, pdf_path, self.period.get())
                    self.generated_files.append(pdf_path)
                
                # 진행률 업데이트
                progress = 10 + int((idx + 1) / total_employees * 90)
                self.progress_var.set(progress)
            
            self.status_label.config(text=f"✅ 완료! {total_employees}명 처리됨")
            self.open_folder_btn.config(state=tk.NORMAL)
            
            # 부드러운 완료 메시지
            messagebox.showinfo(
                "완료", 
                f"급여명세서 생성이 완료되었습니다! 😊\n\n"
                f"처리된 직원: {total_employees}명\n"
                f"생성된 파일: {len(self.generated_files)}개\n\n"
                f"출력 폴더를 열어 확인해보세요!"
            )
            logger.info(f"급여명세서 생성 완료: {total_employees}명, {len(self.generated_files)}개 파일")
            
        except Exception as e:
            error_msg = f"처리 중 문제가 발생했어요! 😊\n\n{str(e)}\n\n로그 파일을 확인해주세요."
            self.error_label.config(text=error_msg)
            messagebox.showerror("오류", error_msg)
            self.status_label.config(text="❌ 오류 발생")
            logger.exception(f"급여명세서 생성 오류: {str(e)}")

if __name__ == "__main__":
    root = tk.Tk()
    app = PayrollApp(root)
    root.mainloop()
```

### 오후 (4시간): 기능 통합 및 에러 처리

#### 1. 모듈 간 연결 확인
- calculator ↔ excel_handler 연결
- excel_handler ↔ pdf_generator 연결
- GUI ↔ 모든 모듈 연결

#### 2. 에러 처리 강화
- 파일 없음 오류
- 잘못된 데이터 형식 오류
- 권한 오류
- 메모리 부족 오류

#### 3. 진행률 표시 개선
- 실시간 진행률 업데이트
- 현재 처리 중인 직원명 표시

---

## 💻 Day 5: PDF 생성 및 테스트 (8시간)

### 오전 (4시간): PDF 생성 기능 개발

#### 1. pdf_generator.py 작성
```python
# pdf_generator.py
from reportlab.lib.pagesizes import A4
from reportlab.pdfgen import canvas
from reportlab.pdfbase import pdfmetrics
from reportlab.pdfbase.ttfonts import TTFont
from reportlab.lib.units import mm
import os
from utils import mask_resident_number, normalize_path
from logger import setup_logger

logger = setup_logger()

class PDFGenerator:
    def __init__(self):
        # 한글 폰트 등록
        font_path = os.path.join(os.path.dirname(__file__), 'assets', 'NanumGothic.ttf')
        if os.path.exists(font_path):
            pdfmetrics.registerFont(TTFont('NanumGothic', font_path))
            self.font_name = 'NanumGothic'
        else:
            self.font_name = 'Helvetica'  # 폴백
    
    def generate_payslip(self, payroll_data, employee_data, output_path, period):
        """급여명세서 PDF 생성"""
        try:
            # 파일 경로 정규화
            output_path = normalize_path(output_path)
            logger.info(f"PDF 생성 시작: {output_path}")
            
            c = canvas.Canvas(output_path, pagesize=A4)
            width, height = A4
            
            # 제목
            c.setFont(self.font_name, 20)
            c.drawString(50*mm, height - 30*mm, "급여명세서")
            
            # 기간
            c.setFont(self.font_name, 12)
            c.drawString(50*mm, height - 45*mm, f"지급기간: {period}")
            
            # 직원 정보
            y_pos = height - 70*mm
            c.setFont(self.font_name, 11)
            c.drawString(50*mm, y_pos, f"성명: {employee_data['이름']}")
            y_pos -= 15*mm
            # 주민번호 마스킹 처리
            masked_rrn = mask_resident_number(employee_data.get('주민번호', ''))
            c.drawString(50*mm, y_pos, f"주민번호: {masked_rrn}")
            
            # 지급 항목 테이블
            y_pos -= 30*mm
            self._draw_table_header(c, 50*mm, y_pos, "지급 항목")
            y_pos -= 20*mm
            
            payments = [
                ("기본급", payroll_data.get('기본급', 0)),
                ("연장근무수당", payroll_data.get('연장근무수당', 0)),
                ("상여금", payroll_data.get('상여금', 0)),
                ("총 지급액", payroll_data['총지급액']),
            ]
            
            for item, amount in payments:
                c.drawString(60*mm, y_pos, item)
                c.drawString(150*mm, y_pos, f"{amount:,}원")
                y_pos -= 15*mm
            
            # 공제 항목 테이블
            y_pos -= 10*mm
            self._draw_table_header(c, 50*mm, y_pos, "공제 항목")
            y_pos -= 20*mm
            
            deductions = [
                ("국민연금", payroll_data['국민연금']),
                ("건강보험", payroll_data['건강보험']),
                ("장기요양", payroll_data['장기요양']),
                ("고용보험", payroll_data['고용보험']),
                ("소득세", payroll_data['소득세']),
                ("지방소득세", payroll_data['지방소득세']),
                ("총 공제액", payroll_data['총공제액']),
            ]
            
            for item, amount in deductions:
                c.drawString(60*mm, y_pos, item)
                c.drawString(150*mm, y_pos, f"{amount:,}원")
                y_pos -= 15*mm
            
            # 실수령액
            y_pos -= 10*mm
            c.setFont(self.font_name, 14)
            c.drawString(50*mm, y_pos, f"실수령액: {payroll_data['실수령액']:,}원")
            
            c.save()
            logger.info(f"PDF 생성 완료: {output_path}")
        except Exception as e:
            logger.exception(f"PDF 생성 오류: {str(e)}")
            raise
    
    def _draw_table_header(self, c, x, y, title):
        """테이블 헤더 그리기"""
        c.setFont(self.font_name, 12)
        c.drawString(x, y, title)
        c.line(x, y - 5, x + 100*mm, y - 5)
```

#### 2. 한글 폰트 준비
- NanumGothic.ttf 다운로드 및 `assets/` 폴더에 배치
- 폰트 라이선스 확인 (상업적 사용 가능)

### 오후 (4시간): 통합 테스트 및 버그 수정

#### 1. 기능 테스트 체크리스트
- [ ] 기본급만 있는 경우 정상 작동
- [ ] 연장근무수당 포함 경우 정상 작동
- [ ] 상여금 포함 경우 정상 작동
- [ ] 엑셀 출력 정상 작동
- [ ] PDF 출력 정상 작동
- [ ] 둘 다 출력 정상 작동
- [ ] 10명 이상 처리 시 성능 확인
- [ ] 한글 파일명 정상 처리

#### 2. 엣지 케이스 테스트
- [ ] 빈 엑셀 파일 처리
- [ ] 필수 컬럼 누락 시 오류 메시지
- [ ] 잘못된 날짜 형식 처리
- [ ] 음수 값 처리
- [ ] 매우 큰 금액 처리 (오버플로우 방지)
- [ ] 특수문자 포함 이름 처리

#### 3. 성능 테스트
- 10명 처리 시간: 목표 5초 이내
- 50명 처리 시간: 목표 20초 이내
- 100명 처리 시간: 목표 40초 이내

---

## 📦 Day 6: 패키징 및 문서화 (8시간)

### 오전 (4시간): exe 파일 빌드

#### 1. PyInstaller 설정 파일 생성
```python
# build.spec
# -*- mode: python ; coding: utf-8 -*-

block_cipher = None

a = Analysis(
    ['main.py'],
    pathex=[],
    binaries=[],
    datas=[
        ('templates', 'templates'),
        ('assets/NanumGothic.ttf', 'assets'),
    ],
    hiddenimports=['matplotlib', 'matplotlib.backends.backend_tkagg'],
    hookspath=[],
    hooksconfig={},
    runtime_hooks=[],
    excludes=[],
    win_no_prefer_redirects=False,
    win_private_assemblies=False,
    cipher=block_cipher,
    noarchive=False,
)

pyz = PYZ(a.pure, a.zipped_data, cipher=block_cipher)

exe = EXE(
    pyz,
    a.scripts,
    a.binaries,
    a.zipfiles,
    a.datas,
    [],
    name='급여명세서생성기',
    debug=False,
    bootloader_ignore_signals=False,
    strip=False,
    upx=True,
    upx_exclude=[],
    runtime_tmpdir=None,
    console=False,  # GUI 모드
    disable_windowed_traceback=False,
    argv_emulation=False,
    target_arch=None,
    codesign_identity=None,
    entitlements_file=None,
    icon='assets/icon.ico',  # 아이콘 파일 필요
)
```

#### 2. 빌드 실행
```bash
# 빌드
pyinstaller build.spec

# 빌드 결과 확인
# dist/급여명세서생성기.exe 생성 확인
```

#### 3. 빌드 최적화
- UPX로 파일 크기 압축 (선택사항)
- 불필요한 모듈 제외
- 파일 크기 목표: 50MB 이하

#### 4. 테스트
- 깨끗한 Windows PC에서 테스트
- 바이러스 백신 오탐 확인 (VirusTotal 사용)
- Windows 10/11 호환성 확인

### 오후 (4시간): 문서 작성

#### 1. 사용자 매뉴얼 작성 (PDF, 10-15페이지)

**목차**
1. 프로그램 소개
2. 시스템 요구사항
3. 설치 방법
4. 엑셀 템플릿 작성 가이드
5. 사용 방법 (단계별 스크린샷 포함)
6. 출력 파일 설명
7. FAQ 및 문제 해결
8. 문의 및 지원

**스크린샷 준비**
- 메인 화면
- 파일 선택 화면
- 진행 중 화면
- 완료 화면
- 생성된 엑셀/PDF 샘플

#### 2. 샘플 파일 준비
- `data/직원정보_샘플.xlsx`: 실제 사용 가능한 샘플
- `output/생성된_급여명세서_샘플.xlsx`: 출력 예시
- `output/생성된_급여명세서_샘플.pdf`: PDF 출력 예시

#### 3. README.md 작성
```markdown
# 급여명세서 자동생성기 v1.0

## 주요 기능
- 4대보험 자동 계산
- 소득세/지방소득세 자동 계산
- 엑셀/PDF 자동 생성
- 직원별 개별 명세서 생성

## 설치 방법
1. `급여명세서생성기.exe` 실행
2. 직원 정보 엑셀 파일 준비
3. 생성하기 버튼 클릭

## 문의
이메일: support@example.com
```

---

## 🚀 Day 7: 출시 준비 및 마케팅 (8시간)

### 오전 (3시간): 판매 페이지 작성

#### 1. 크몽 상품 등록

**제목**
```
[급여 자동화] 클릭 한 번으로 급여명세서 100건 생성! 🚀
```

**상품 설명**
```
✅ 급여 계산부터 PDF 출력까지 5분 완성
✅ 4대보험, 소득세 자동 계산 (2025년 최신 세율 반영)
✅ 엑셀 템플릿 3종 무료 제공
✅ 직원별 개별 명세서 자동 생성
✅ 평생 무료 업데이트
✅ 1:1 설치 지원 (카카오톡/이메일)

📋 제공 파일
- 프로그램 exe 파일 (Windows 10/11 호환)
- 사용자 매뉴얼 PDF (15페이지)
- 엑셀 템플릿 3종 (기본형/상세형/간략형)
- 샘플 데이터 파일
- 동영상 사용법 (5분)

💰 가격
- 오픈 기념 특가: 29,900원 (정가 39,000원)
- 첫 50명 한정 특가

⚠️ 주의사항
- 본 프로그램은 세무 자문을 대체하지 않습니다.
- 복잡한 세무 상황은 세무사와 상담하시기 바랍니다.
- 14일 이내 환불 보장 (미사용 시)

📞 문의
카카오톡: @급여자동화
이메일: support@example.com
```

#### 2. 상세 페이지 구성
- 제품 썸네일 이미지 (Canva 활용)
- 스크린샷 10장 (주요 기능별)
- 사용 전/후 비교 이미지
- 고객 후기 (지인 테스트 활용)

### 오후 (3시간): 마케팅 자료 제작

#### 1. 이미지 자료
- **썸네일**: 800x800px, 제목 + 핵심 기능 3가지
- **상세 이미지**: 각 기능별 스크린샷 + 설명
- **비교 이미지**: 수작업 vs 자동화 (시간 절감 강조)

#### 2. 동영상 매뉴얼 (선택사항)
- OBS Studio로 화면 녹화
- 5분 이내 사용법 설명
- 자막 추가

#### 3. 블로그 포스팅 (선택사항)
- "급여명세서 자동화로 시간 절약하기" 주제
- 네이버 블로그/티스토리 작성
- 크몽 링크 포함

### 저녁 (2시간): 최종 점검 및 출시

#### 1. 최종 점검 체크리스트
- [ ] exe 파일 바이러스 검사 완료 (VirusTotal)
- [ ] 깨끗한 PC에서 설치 테스트 완료
- [ ] 매뉴얼 오타 검수 완료
- [ ] 샘플 파일 정상 작동 확인
- [ ] 가격 및 결제 설정 확인

#### 2. 판매 채널 등록
- 크몽 등록
- 오픈마켓 (네이버 스마트스토어 등) 등록
- 블로그 링크 판매 준비

#### 3. SNS 홍보
- 네이버 카페 게시글 작성
- 관련 페이스북 그룹 공유
- 지인들에게 테스트 요청 및 후기 수집

---

## 📊 일정 요약표

| 일차 | 주요 작업 | 산출물 | 체크리스트 |
|------|----------|--------|-----------|
| Day 0 | 개발 환경 세팅 | 프로젝트 구조, 가상환경 | [x] 프로젝트 폴더 생성 ✅<br>[x] 패키지 설치 완료 ✅<br>[x] logs 폴더 생성 ✅ |
| Day 1 | 요구사항 정의 | 기능 명세서, 데이터 구조 설계 | [ ] 타겟 고객 인터뷰 3명<br>[ ] 경쟁사 분석 완료<br>[x] 엑셀 템플릿 작성 ✅<br>[x] 보안/UX 개선 사항 반영 ✅ |
| Day 2 | 기술 스택 확정 | config.py, calculator.py, logger.py, utils.py, dashboard.py | [x] Git 저장소 생성 ✅<br>[x] 로깅 시스템 구축 ✅<br>[x] 보안 함수 구현 ✅<br>[x] 대시보드 모듈 기본 구조 ✅<br>[x] 핵심 모듈 기본 구조 완성 ✅ |
| Day 3 | 계산 로직 완성 | calculator.py 완성, excel_handler.py | [x] 단위 테스트 통과 ✅<br>[ ] 엑셀 출력 기능 완성<br>[x] 미리보기 기능 구현 ✅ |
| Day 4 | GUI 개발 | main.py 완성 (탭 구조 + 대시보드) | [ ] 탭 구조 구현 (main.py 통합 필요)<br>[x] 대시보드 UI 구현 ✅ (view_dashboard.py)<br>[x] 월급여 정산현황 막대 그래프 구현 ✅<br>[x] 근무자구성 도넛 그래프 구현 ✅<br>[x] GUI 모든 버튼 동작 확인 ✅ (대시보드)<br>[x] 인라인 오류 메시지 구현 ✅ (대시보드)<br>[x] 미리보기 UI 구현 ✅ (대시보드)<br>[x] 완료 후 파일 열기 버튼 ✅ (대시보드)<br>[x] 에러 처리 완료 ✅ (대시보드) |
| Day 5 | PDF 생성 | pdf_generator.py 완성 | [ ] PDF 한글 출력 확인<br>[x] 주민번호 마스킹 처리 ✅ (utils.py)<br>[ ] 통합 테스트 완료 |
| Day 6 | 패키징 | exe 파일, 사용자 매뉴얼 | [ ] exe 빌드 성공<br>[ ] 타 PC 테스트 완료<br>[x] 로그 파일 확인 ✅<br>[ ] 매뉴얼 작성 완료 |
| Day 7 | 출시 준비 | 판매 페이지, 마케팅 자료 | [ ] 크몽 등록 완료<br>[ ] 최종 점검 완료<br>[ ] 보안 검증 완료 |

---

## 💰 예상 수익 시뮬레이션

### 보수적 예상
- **첫 달**: 10건 × 29,900원 = **299,000원**
- **3개월 후**: 월 30건 × 39,000원 = **1,170,000원/월**
- **수정 요청 대응 시간**: 월 10시간

### 공격적 마케팅 시
- 블로그/유튜브 콘텐츠 제작
- 네이버 카페 적극 홍보
- **첫 달 30건 이상 가능** → **897,000원**

---

## 🎯 핵심 성공 포인트

### 1. 범위 제한
- MVP에 꼭 필요한 기능만 포함
- v2.0에서 추가 기능 확장

### 2. 기술적 선택 이유
- **tkinter**: 별도 설치 없이 Python 내장, 빌드 용량 최소화
- **openpyxl**: 가장 안정적인 엑셀 라이브러리
- **PyInstaller**: 단일 exe 생성, 배포 편의성

### 3. 차별화 포인트
- **즉시 사용 가능**: 설치 후 5분 내 첫 급여명세서 생성
- **템플릿 3종 제공**: 기본형, 상세형, 간략형
- **한글 최적화**: 모든 메시지와 문서가 한글
- **평생 사용**: 월 구독료 없음

### 4. 리스크 대응
- **법적 책임**: 면책 조항 명시 (세무사 자문 권장)
- **환불 정책**: 14일 환불 보장으로 신뢰 확보
- **기술 지원**: 이메일/카카오톡 지원 명시 (1주일 내 답변)

---

## 📝 개발 중 주의사항

### 1. 세율 및 상한액
- 2025년 최신 세율 반영 필수
- 매년 업데이트 필요 (v1.1에서 반영)

### 2. 데이터 보안 (🔴 MVP 필수)
- **주민번호 마스킹 처리**: PDF 출력 시 뒷자리 마스킹 (`*******`)
- **파일 경로 정규화**: `os.path.abspath`, `os.path.normpath` 사용
- **로깅 시스템**: 모든 오류와 주요 작업을 `logs/app.log`에 기록
- **로컬 처리만**: 클라우드 업로드 없이 로컬에서만 처리
- **주민번호 저장 최소화**: 앞 6자리만 저장, 전체 번호는 메모리에서만 사용

### 3. 사용자 경험 (🔴 MVP 필수)
- **직관적인 GUI 설계**: 탭/프레임 단위로 명확히 구분
- **엑셀 파일 미리보기**: 파일 선택 후 첫 3행 미리보기 제공
- **인라인 오류 메시지**: messagebox 대신 화면 내 경고 라벨 사용
- **부드러운 오류 메시지**: "입력 형식에 문제가 있어요! 😊" 형식
- **완료 후 액션**: "파일 열기 / 폴더 열기" 버튼 제공
- **진행 상태 실시간 표시**: 진행률 바 + 현재 처리 중인 직원명

### 4. 테스트
- 다양한 엣지 케이스 테스트
- 실제 사용자 시나리오 기반 테스트
- 성능 테스트 (대량 데이터 처리)

---

## 🔄 v1.1 개선 사항 (출시 후 1개월 내)

### 기술적 개선
1. **Service Layer 추가**
   - `PayrollService` 클래스로 비즈니스 로직과 GUI 완전 분리
   - 테스트 용이성 향상

2. **자동 테스트 구축**
   - pytest 기반 단위 테스트
   - 통합 테스트 자동화
   - GitHub Actions CI/CD 파이프라인 구축

3. **성능 최적화**
   - ThreadPoolExecutor로 PDF 생성 병렬 처리 (40~50% 속도 개선)
   - pandas + openpyxl I/O 효율화
   - 메모리 누수 방지 (with 컨텍스트 관리 강화)

### UX 개선
4. **브랜드 디자인 적용**
   - 브랜드 컬러: Deep Blue (#304FFE) + Soft Yellow (#FFD54F)
   - 버튼 디자인 개선 (3D 그림자 또는 라운드 버튼)
   - 아이콘 추가 (📁, ⚙️, ✅)
   - 진행 상태를 단계별 아이콘 플로우로 변경

5. **접근성 개선**
   - WCAG 기준 대비율 4.5:1 이상 확보
   - 폰트 크기 12~14pt 권장
   - Tab 키 포커스 순서 명확히 지정

---

## 🔄 v2.0 로드맵 (향후 개선 사항)

### 기능 확장
1. **연말정산 기능**
2. **이메일 자동 발송**
3. **회사 로고 삽입 기능 강화**
4. **급여 이력 관리 (DB 연동)**
5. **다국어 지원 (영어)**
6. **복잡한 변동 수당 계산**

### 마케팅 및 운영
7. **무료 체험판 제공**
   - 3일 체험 버전 또는 리뷰용 체험판
   - "10명 무료 테스트 → 후기 확보 → 유료 전환" 전략

8. **고객 피드백 루프 설계**
   - 사용자 피드백 수집 시스템
   - 개선 사항 우선순위 결정 프로세스

9. **브랜드화 전략**
   - 브랜드명 확정 (예: PayGenie)
   - 일관된 브랜드 아이덴티티 적용

10. **마케팅 확장**
    - 블로그 SEO 기반 콘텐츠 마케팅
    - 네이버 카페 "사장님 커뮤니티" 협업 게시글 활용
    - 유튜브 튜토리얼 영상 제작

---

## 📞 지원 및 문의

- **이메일**: support@example.com
- **카카오톡**: @급여자동화
- **응답 시간**: 평일 1주일 이내

---

## 📝 종합 분석 반영 사항

이 실행 계획은 **종합 분석 문서**의 전문가 검증 결과를 반영하여 업데이트되었습니다.

### 🔴 MVP 출시 전 필수 개선 사항 (반영 완료)
- ✅ **대시보드 기능** (메인 첫 페이지) ✅
  - ✅ 현재 직원 수, 총 급여, 총 공제, 총 실수령액 표시 ✅
  - ✅ **월급여 정산현황** 막대 그래프 (직원별 실수령액) ✅
  - ✅ **근무자구성** 도넛 그래프 (정규직/계약직/신입 비율) ✅
  - ✅ 특이사항 표시 (연장근무, 상여금 등) ✅
- ✅ 주민번호 마스킹 처리 (PDF 출력 시) ✅ (utils.py 구현 완료)
- ✅ 로깅 시스템 추가 (`logs/app.log`) ✅
- ✅ 파일 경로 정규화 및 검증 ✅
- ✅ 엑셀 파일 미리보기 기능 (첫 3행) ✅
- ✅ 완료 후 "파일 열기" 버튼 추가 ✅ (view_dashboard.py에 구현)
- ✅ 인라인 오류 메시지 표시 ✅ (view_dashboard.py에 구현)
- ✅ 부드러운 오류 메시지 문구 적용 ✅ (view_dashboard.py에 구현)

### 🟡 v1.1 개선 사항 (로드맵 추가)
- Service Layer 추가 (PayrollService)
- pytest 기반 자동 테스트
- 병렬 처리로 성능 최적화
- GitHub Actions CI/CD
- 브랜드 컬러 및 아이콘 적용
- 단계별 진행 상태 표시
- 접근성 개선 (대비율, 폰트 크기)

### 🟢 v2.0 로드맵 (마케팅 전략 추가)
- 무료 체험판 제공
- 고객 피드백 루프 설계
- 브랜드화 전략 (PayGenie 등)
- 블로그 SEO 콘텐츠 마케팅

**예상 개선 효과**: 종합 점수 82점 → **90점+ 달성 가능**

---

**마지막 업데이트**: 2025-01-XX  
**버전**: 1.1 (종합 분석 반영)  
**라이선스**: 상업적 사용 가능

